# C. topLevel 与层级边界 — dev + user

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| C1 | topLevel=H2，`# 篇`/`## 节` | `# 篇`（不编号）/ `## 1 节` | ✅ |
| C2 | C1 基础上 topLevel 改 H2 → H1，再触发 | `# 1 篇` / `## 1.1 节` | ✅ |
| **C3** | C2 之后再把 topLevel 改回 H1 → H2，再触发 | `# 篇`（去掉 `1 `）/ `## 1 节` | ✅（0.6.0：`level<top` 分支调用 `stripHeadingPrefix`；根因：对 `< topLevel` 标题不剥离，调高 topLevel 后旧前缀成孤儿）|
| C4 | topLevel=H3，`## 概述`/`### 细节` | `## 概述` 不编号 / `### 1 细节` | 🔲 |
| C5 | topLevel=H1，多个 H1 | `# 1 …`/`# 2 …`，各 `##` 子节从 `x.1` 起 | 🔲（spec §3.4 已定预期）|
| **C6** | topLevel=H3，H2 含 `## 1 2024 总结`，反复触发 | 首次一次到定点 `## 总结`，之后稳定幂等 | ✅（0.6.3：低于 topLevel 分支改为循环剥离到定点，见 U1）|
| **C7** | bottomLevel=H4，H2/H3/H4/H5/H6 各有标题 | H2–H4 编号，H5/H6 原样不动 | ✅（0.6.5：`level > bottom` 走非编号分支）|
| **C8** | 先 bottomLevel=H6 编号到 H5，再收窄到 H4，再触发 | H5 旧前缀被剥成裸标题（对称 C3） | ✅（0.6.5 循环剥离）|
| **C9** | bottomLevel=H3，`## 章`/`### 节一`/`#### 深`/`### 节二`，反复触发 | H4 超界不编号但仍作重置边界；`### 节二` 得 `1.2`；幂等 | ✅（0.6.5）|

> **C3 根因**：`numberHeadings` 对 `< topLevel` 的标题原本不剥离（避免误伤裸标题），导致 topLevel 调高后旧前缀成孤儿。0.6.0 修复：新增剥离分支，**只剥明显是插件前缀的部分**（与 M6 cleanup 剥离器共用）。
