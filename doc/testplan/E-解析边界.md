# E. 解析边界 — dev + user

> 双哨兵的愈合边界（首哨兵作结构性证据、首尾均毁不自愈、不采用「精确去重」）见 [spec §2.5](../spec/2.5-双哨兵自愈.md)。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| E1 | 围栏代码块内的 `# 注释` | 不当作标题、不编号 | ✅ |
| E2 | `~~~` 围栏 + 内部 `#` | 同 E1（需同种符号闭合） | ✅ |
| E3 | 反引号开、波浪号"闭"（不匹配） | 视为未闭合，块内 `#` 仍跳过 | 🔲 |
| E3b | 同符号反引号数量不匹配（开 4 根、内层写 3 根，嵌套围栏写法） | 数量不足不闭合，整块仍视为未闭合直至真正 ≥4 根的收尾行；内外层 `#` 均跳过、不推进计数器 | ✅（issue #9，`parser.test.ts` + `known_bugs.test.ts` 回归）|
| E4 | 空标题行（`### ` 无字），反复触发 | `### 1.1 `（末尾即间隔符）；幂等，不叠成 `1.1 1.1` | ✅（rawText 修复）|
| E5 | 手写 `## 2024 总结`（arabic），**首次**编号 | `2024` **完整保留**：得 `## 1 ⁠2024 总结`（无 WJ → 整段是正文，不剥），幂等 | ✅（0.6.6 方案A 根治，`known_bugs.test.ts` U1/U2 回归）|
| E5b | E5 结果后补回数字：`## 1 ⁠2024 总结` 再触发（含 WJ） | **稳定保留** `## 1 ⁠2024 总结`，完全幂等 | ✅（0.6.4 WJ 快速路径根治）|
| E6 | 标题恰为序号字样 `## 三`（cjk，末尾无空格） | 不误剥，编号成 `## 一 三` | ✅（rawText 修复）|
| E7 | `##\t标题` / `##   标题` | 识别为 H2，正常编号 | 🔲 |
| E8 | 行首带 ≤3 空格 `  ## 标题` | 当前正则 `^#` 不匹配前导空格 → 不编号 | ⚠️/🔲（确认是否支持，记录现状）|
| E9 | `####### 七个井号` | 非合法标题（>6），不编号 | ✅ |
| E10 | Setext 标题（`标题\n===`） | 不支持（仅 ATX），不编号 | ⚠️（非目标）|
| E11 | frontmatter `#`（YAML 注释）/ 正文 `#标签`（无空格） | 不当作标题 | ✅ |
| E12 | `upper-alpha` 级，标题以同字母段起头（`## API 设计`） | `API` **完整保留**：得 `## A ⁠API 设计`（无 WJ 不剥），幂等 | ✅（0.6.6 方案A 根治 U3，`known_bugs.test.ts` 回归）|
| **E13** | 标题含 Word Joiner `1 ⁠2024 总结`（0.6.4 `buildPrefix` 写入） | `stripPrefix` WJ 快速路径截到标记处，返回 `2024 总结`；再触发稳定幂等 | ✅（0.6.4）|
| **E14** | **用户报告 bug**：`## 一、⁠标题2` 从 `、` 开始删（连同不可见尾哨兵 WJ 一起删）→ `## ⁠一标题2`，再触发 | **愈合**回 `## ⁠一、⁠标题2`，不叠成 `一、⁠一标题2`（首哨兵作证据、有界剥离孤儿序号）；幂等 | ✅（0.7.20 双哨兵，`known_bugs.test.ts` E14）|
| **E15** | 仅删尾哨兵 WJ、后缀 `、` 尚在（`## ⁠一、标题2`），再触发 | 愈合回 `## ⁠一、⁠标题2`，不叠成 `一、一、标题` | ✅（0.7.20，`known_bugs.test.ts` E15）|
| **E16** | 把 `## ⁠一、⁠标题2` 的 `#` 删光降级为正文（`⁠一、⁠标题2`），再触发 | ③ 残留清理：正文行的 WJ 哨兵 + 编号被清净 → `标题2`；非 WJ 起头正文 / 围栏内容不误伤 | ✅（0.7.20，`known_bugs.test.ts` E16/E16b）|
| **E17** | 降级残留正文 + 跑「清除编号」命令 | 与 ③ 一致：`clearNumberingContent` 也清正文残留（不留 `⁠一、⁠乙` 垃圾） | ✅（0.7.20，`known_bugs.test.ts` E17）|
| **E18** | 双哨兵不回归方案 A：`## 2024 总结` 首次编号 | `2024` 完整保留 `## ⁠1 ⁠2024 总结`，幂等（自愈仅在**有首哨兵证据**时触发，无 WJ 仍一律正文） | ✅（0.7.20，`known_bugs.test.ts` E18）|
| **E19** | 注释块跳过（M12）：`%%` 单独一行开、中间 `## 标题`、`%%` 单独一行闭 | 块内 `#` 行**不当作标题**：不编号、不推进计数器、不进 backlink 快照 | ✅（1.0.14） |
| **E20** | 同 E19 但用 `<!--` / `-->` | 同 E19 | ✅（1.0.14） |
| **E21** | R3：`## 标题 %% 批注 %%`（行内开闭，标题在注释左侧） | **是标题**（行首第 0 列不在注释内）；注释原文留在 `rawText`，编号写在其左侧；二次触发幂等 | ✅（1.0.14） |
| **E22** | R4：`## 标题 <!--` 开启未闭合注释，下一行 `## 被遮蔽`，再下一行 `-->`，其后 `## 恢复` | 本行**是**标题；`<!--` 与 `-->` 之间的 `#` 行被遮蔽；`-->` 之后恢复识别 | ✅（1.0.14） |
| **E23** | R5 优先级：围栏内出现 `%%`（不应开启注释）；反向——注释内出现 ``` ``` ```（**会**开启围栏） | 正向：围栏后的标题仍被正确识别（证明无状态泄漏）。反向列为**已知限制**，本行钉住现状——失败方向是「多跳过 / 冻结」而非「误编号」，安全 | ✅（1.0.14） |
| **E24** | R6：行内代码 `` `<!--` `` 里的分隔符 | **已知限制，不处理**：仍会开启注释（正确处理需 CommonMark 级反引号游程 tokenizer，与收益不成比例）。失败面是下方标题冻结、可见可恢复，不是往用户以为隐藏的地方写垃圾 | ✅（1.0.14） |
| **E25** | R7：未闭合 `%%` / `<!--` 一直到文件末尾 | 与未闭合围栏对称：下方全部冻结。`renumberContent` 不剥已有前缀 ⇒ 是**冻结而非损坏**，补上闭合符即完全恢复 | ✅（1.0.14） |
| **E26** | R8/R10 边界：`<!-- <!-- -->`（不嵌套，首个 `-->` 即闭合）；`%%%`（贪婪左到右 = 开标记 + 残留 `%`）；`%%%%` | 按 R8/R10 裁决，静态用例钉死 | ✅（1.0.14） |
| **E27** | 计数器不受注释块影响：`## A` / `%%`+`## B`+`%%` / `## C` | C 得 `2` 而非 `3`——注释内标题**不占计数器槽位**（「不能影响正文编号」） | ✅（1.0.14） |
| **E28** | **分区域残留清理**（M12 决策）：已编号标题事后被包进 ① 注释块 ② 围栏代码块，各自触发自动重编号 | ① 注释块内：WJ 前缀**被自动清掉**（注释是隐藏散文，清掉插件自己的不可见残留是安全的）；② 围栏内：**原样冻结不动**（代码是字面内容，日常编辑绝不改写——否则演示本插件 WJ 格式的代码块会被静默吃掉） | ✅（1.0.14） |
| **E29** | 同 E28 两种区域，改跑「清除当前文件编号 / 清除全库编号」 | **两种区域都清净**——否则注释内不可见的 WJ 残留用户肉眼永远找不到，跑完清除仍被污染，破坏标记契约「永远可退出 / 精确剥净」的承诺。区域内**无 WJ** 的普通文本一个字符不动 | ✅（1.0.14） |
| **E30** | 单标题跳过（issue #6，spec §3.21）：`## 甲` / `## 临时讨论 <!-- skip -->` / `## 乙` | 中间那条**不编号**，且**不占计数器槽位** ⇒ 乙 得 `2` 而非 `3` | ✅（1.0.17 dev 单测） |
| **E31** | 给**已编号**的标题事后补上 `<!-- skip -->`，触发重排 | 该标题的编号被**摘掉**（不是冻结在原地留下永不更新的僵尸编号），其槽位随之让给后续标题 | ✅（1.0.17 dev 单测） |
| **E32** | 标记形态容错：`<!--skip-->` / `<!--   SKIP   -->` / `<!-- Skip -->` | 三者均命中（大小写不敏感、容忍内部空白） | ✅（1.0.17 dev 单测） |
| **E33** | 标记出现在标题**中间**：`## <!-- skip --> 甲` | **不生效**，照常编号——中间位置的同形字符串多半是用户在写批注，不该被当成指令 | ✅（1.0.17 dev 单测） |
| **E34** | 被跳过标题**下面**挂子标题：`## 甲` / `## 临时讨论 <!-- skip -->` / `### 子项` / `## 乙` | phase 1 **只跳过本行、不含子树**：子项挂在「甲」的槽位下得 `1.1`，乙 得 `2` | ✅（1.0.17 dev 单测；是否加「连子树一起跳」选项待真实反馈） |
| **E35** | 幂等 + 正文同形串：`## 甲 <!-- skip -->` / 正文行含 `<!-- skip -->` / `## 乙` | 连跑两次结果一致；正文行一个字符不动；乙 得 `1` | ✅（1.0.17 dev 单测） |
| **E36** | **实机**：带 `<!-- skip -->` 的标题，在真实 vault 里用 `[[文件#` 触发标题补全、并检查已有内链是否解析 | 记录 Obsidian 的 metadataCache 把 HTML 注释算不算进标题锚点、补全给出哪一种形态。当前实现**不对锚点做特殊处理**（与 Obsidian 原始文本口径一致），实测后按结论回填本行、必要时再定是否特殊处理 | 🔲 **待用户真机手验**（spec §3.21「未定项」第三条） |
| **E37** | **标题中间含 WJ**：典型来源是指向已编号标题的链接（锚点带 WJ）：`## 参见 [[a#⁠1 ⁠概述]]`，触发编号 | 链接一字不丢：`## ⁠1 ⁠参见 [[a#⁠1 ⁠概述]]`，再触发幂等 | ✅（1.1.5，`numbering` / `known_bugs` / `cleanup` 测试回归；根因：「首个 WJ 不在位置 0」一律被当旧单哨兵剥掉，而链接锚点也带 WJ——判定规则见 spec §2.5）|
| **E38** | 同 E37，但该标题**超出编号区间**（默认模板 `# 书 [[a#⁠1 ⁠概述]]` + `## 甲`，H1 走 bareHeading 定点剥离） | H1 原样保留 `# 书 [[a#⁠1 ⁠概述]]`，不被定点循环逐层蚕食成 `# 概述]]` | ✅（1.1.5，`numbering` / `known_bugs` / `cleanup` 测试回归） |
| **E39** | 尾哨兵被毁 + 正文含链接 WJ：`## ⁠1参见 [[a#⁠1 ⁠概述]]`（E14 的删后缀操作发生在带链接的标题上） | 第二个 WJ 属于链接锚点、**不是**尾哨兵 → 走有界剥离愈合回 `## ⁠1 ⁠参见 [[a#⁠1 ⁠概述]]`，链接完好 | ✅（1.1.5，`numbering` / `known_bugs` / `cleanup` 测试回归） |
| **E40** | 命令路径遇中间 WJ：① 「清除编号」`## ⁠1 ⁠参见 [[a#⁠1 ⁠概述]]` / `# 书 [[a#⁠1 ⁠概述]]`；② 「清理非本插件编号」手写 `## 1. 参见 [[a#⁠1 ⁠概述]]`；③ 迁移守卫：从未编号、手写 `## 1 红米` 的文件正文里有一条带 WJ 的链接 | ① 只剥真前缀，链接保留；② 被识别为外来编号剥掉 `1. `（不因链接里有 WJ 就当「插件的」跳过）；③ 守卫照常拦截（链接里的 WJ 不是「插件接触过」的证据） | ✅（1.1.5，`numbering` / `known_bugs` / `cleanup` 测试回归） |
| **E41** | 旧单哨兵兼容不回归：`## 1 ⁠标题` / `## 第1章 ⁠标题`（前缀字面量在候选集内）再触发 | 首个 WJ 之前**整段**是编号形态 → 仍按旧格式剥净升级为双哨兵；整段不像编号（含链接语法等）→ 视为正文不剥（宁可不剥也不吃正文） | ✅（1.1.5，`numbering` / `known_bugs` / `cleanup` 测试回归） |
