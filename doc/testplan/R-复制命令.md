# R. 复制命令：编号大纲 / 当前小节链接（1.2.0） — dev + user

> 两种模式都能用（写入模式取标题所见文本、仅显示模式取虚拟编号）。文本拼装纯函数单测，剪贴板与命令可用性走 `main.test.ts`，链接解析与同步留真机。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| R1 | 写入模式的已编号文件，执行「复制编号大纲」 | 剪贴板里每个标题一行，内容即标题所见文本（编号 + 标题，WJ 剥净）；按层级缩进（最浅层级不缩进，每深一级两个空格）；围栏 / 注释里的 `#` 行不算标题；提示已复制几个标题 | ✅（`copycommands.test.ts` `buildNumberedOutline` 写入模式 + 围栏/注释跳过 + `main.test.ts` 命令层「写入模式取标题所见文本」） |
| R2 | 仅显示模式文件执行「复制编号大纲」 | 编号取虚拟编号，与编辑器里看到的一致；不编号的标题（超出编号区间、白名单跳过）只有文字；带残留旧编号的标题输出「虚拟编号 + 标题」，不带残留 | ✅（`copycommands.test.ts` `buildNumberedOutline` 传入虚拟编号 3 例 + `main.test.ts` 仅显示文件命令层，与编辑器所见一致且文件字节不变） + 真机（仅显示：与编辑器逐条一致，无不可见字符） |
| R3 | 没有标题的文件执行「复制编号大纲」；在阅读视图里执行 | 前者只提示没有标题，不动剪贴板；后者照常可用（不要求处于编辑模式） | ✅（`copycommands.test.ts` count 为 0 + `main.test.ts` 没有标题只提示不写剪贴板；用 `checkCallback` 而非 `editorCallback` 保证阅读视图可用，见 `registerCopyCommands`） + 真机（无标题只提示、剪贴板不变；阅读视图下照常复制） |
| R4 | 光标放在某小节正文里（或标题行上）执行「复制当前小节链接」 | 复制光标所在小节（光标行或其上方最近的标题）的链接：按用户的链接设置生成（默认 `[[文件#锚点\|标题]]`），锚点与标题字节一致（写入模式保留 WJ），显示文字是不带编号的标题 | ✅（`copycommands.test.ts` + `main.test.ts`；真机：仅显示 `[[基础#背景\|背景]]`；链接解析与改名同步见 R6）|
| R5 | 光标在第一个标题之前；文件没有标题 | 「复制当前小节链接」不出现在命令面板 | ✅（`copycommands.test.ts` `sectionHeadingAt` 两种情形均返回 `null` + `main.test.ts` `editorCheckCallback(true, …)` 返回 `false`） + 真机（无标题笔记里命令面板「未发现命令」） |
| R6 | 把 R4 复制的链接粘贴进另一篇笔记，再回原文件改该标题、或在它前面插入标题让编号变化 | 链接能解析、点击跳到该小节；标题变化后链接被「改名不断链」同步（写入与仅显示两种模式都成立） | ✅ 仅显示模式真机（复制的链接粘贴进另一篇，改名后锚点同步、仍可跳转）/ 🔲 写入模式真机 |
