# J. 触发、防抖与写回 — dev + user

> J1–J5 由 `main.test.ts` 覆盖（经 `obsidian-mock.ts` + vitest 别名加载真实插件，用假编辑器 + 假定时器驱动）。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| J1 | 连续快速输入 | 防抖（默认 300ms）停顿后触发一次 | ✅（0.3.18）|
| J2 | 防抖到期前关闭文件 | 取消待处理更新，不写已关闭文件 | ✅（0.3.18）|
| J3 | 多文件交替编辑 | 防抖以文件为单位，互不取消 | ✅（0.3.18）|
| J4 | 写回用单一事务 | 一次撤销即可回退整次重排 | ✅（0.3.18）|
| J5 | 在设置面板改模板后 | 当前活动文件即时重排 | ✅（0.3.18）|
| J6 | 编号写回不应移动光标/打乱选区 | 光标位置合理保留 | 🔲（假编辑器不建模选区，留手验）|
| J7 | 「立即重新编号」绕过防抖、取消同文件待处理更新 | 立即编号一次；防抖被取消不二次触发；开关关时弹 Notice 不动 | ✅（0.3.18）|
| **J8** | **IME 组合期间防抖顺延**（0.7.17）：拼音组合中防抖到点 | 不写回（避免把组合中的拼音编入标题），顺延一个防抖周期；`compositionend` 后下一周期正常写回 | ✅（`main.test.ts` J8，document 级 compositionstart/end 维护 `imeComposing`）|
| **J9** | **用户需求**：路径规则改投了模板（或模板本身改了样式）后，该路径下**尚未打开过**的文件——只打开、不做任何编辑 | 打开即按当前生效模板自动重排（复用与实时编辑一致的门控：`shouldAutoTrigger` + 按路径解析模板）；已是最新格式的文件打开时静默 no-op（`applyRenumber` 内容未变不写入，不产生多余撤销记录）；全局开关关且非 fm:true，或 fm:false 时打开不触发 | ✅（0.7.24：`file-open` 事件新增 `renumberOnOpen`，`main.test.ts` 回归）|
| **J12** | **迁移守卫误伤**（1.0.15 修，J11 的用例把它挖了出来）：一个**全文无 WJ、也无任何编号**的文件，某个标题行尾带空白（`## 标题 `，最常见于用户刚敲下空格） | 守卫**不该**命中。此前 `hasUnclaimedForeignNumbering` 拿 `stripForeignNumbering(rawText)` 与 `rawText` 直接比，而前者末尾带一道 `\s+$` 归一化 ⇒ 仅仅因为行尾有空格就判定「像外来编号」，该文件的自动编号被整个拦下、并弹出误导性的「请先执行『清理非本插件的标题编号』」。修法：比较基准同样去掉行尾空白，守卫只对**真的剥掉了编号**的情形生效 | ✅（1.0.15）|
| **J11** | **打字被打断**（1.0.15 修，**1.0.23 换手段**）：防抖到期时光标正停在某个**将被改写**的行上——最常见的是在标题文本末尾敲了空格、正准备继续输入 | **只把用户刚敲的行尾空白补回本轮结果**，**编号照常写入**（1.0.23 起）：空格不被 `stripPrefix` 的 `\s+$` 归一化吃掉（`strip.ts:262/266/275`），但也不再因此扣住整行的编号。光标停着不动连跑两轮时第二轮无改动可写（补回空白后与落盘内容逐字节相同，不发空事务）；光标移开后那一轮空白按既有规则清掉。快照按实际落盘内容记录，补回的空白不产生幻影改名（`parseHeadings` 的 `text` 本就去尾空白）。**手动命令**（立即重新编号 / 打开文件即重排 / 改模板即时重排）**不受保护**，行尾空白照常归一化 | ✅（1.0.15 初版整行冻结 → 1.0.23 开发中一度改用层级判据 → **1.0.23 最终改为只补行尾空白**，见 J19；`main.test.ts` 5 例）|
| **J10** | **迁移体验**：从其他编号插件 / 手写编号迁移过来的文件（全文无 WJ）第一次被自动路径（编辑防抖 / 打开文件 / 改模板后即时重排）碰到，且标题被 `stripForeignNumbering` 判定为「像外来编号」（如 `## 1 红米`） | 方案A下自动路径只认 WJ、把 `1 红米` 当纯正文再叠自己的编号，会产生 `## 1 1 红米` 这种观感上像 bug 的双重编号——**跳过本次自动写入**，改为可点击的 Notice 提示；同一次 file-open 内只提示一次（避免打字连续触发刷屏，重新打开该文件后允许再提示一次，见 J13）；**手动命令**（立即重新编号 / 清除编号 / 清理外来编号）**不受此守卫影响**，绕过一切开关照常执行（与既有「Renumber now 绕过一切开关」原则一致） | ✅（1.0.7：`hasUnclaimedForeignNumbering`，`cleanup.test.ts` + `main.test.ts` 回归；重新提示 / Notice 可点击见 J13/J14）|
| **J13** | **迁移守卫提示只为「用户当前正看着的文件」发声**：① 在 a.md（外来编号）里打字后、防抖到期**前**切到 b.md；② 改模板触发批量刷新（遍历全部打开叶子，含后台脏文件）；③ 同一文件里连续打字、防抖反复到期；④ 切到别的文件；⑤ 切走再切回；⑥ 文件被清理干净 | ①**不**弹提示（那条说的是 a.md，用户眼前是 b.md，弹了会被读成在说 b.md）；②只有当前活动的那个疑似文件弹，后台的不弹；③屏幕上**始终只有一条**，不堆叠不闪烁（Notice 是 `duration:0` 不自动消失的，重建会把用户正要点的那条抽掉）；④上一条被**主动收起**，不留指向另一篇笔记的孤儿提示；⑤重新提示（1.0.17 反馈的原始诉求）；⑥主动收起（告警已不成立） | ✅（**1.0.21 用户真机确认通过**；实现见 **1.0.19 重做**：1.0.17 按 `file-open` 事件重置「已提示」标志，1.0.18 改按「最近一次检查的路径」推导，两版都漏了根因——**守卫检查的发起方不保证是用户正看着的文件**：`scheduleRenumber` 的计时器捕获的是安排那一刻的路径、`renumberActiveFile` 直接遍历全部叶子。真机症状「打开外来编号文件不弹 / 切到另一个文件反而弹 / 点进去说没什么可清理」三条同源。现由 `showForeignNumberingGuardNotice` 统一裁决：`getActiveFile()` 校验 + 单条 `activeGuardNotice` 按路径去重 + 失效即 `hide()`。`main.test.ts` 回归 6 例，含真机链路复现；测试替身补 `getActiveFile`——**旧替身缺这个方法正是两版都没测出来的原因**） |
| **J14** | **迁移守卫 Notice 可点击 → 清理预览确认框**：J10 命中后弹出的 Notice 上点击可点击文案（不做无预览的一键清理——清理命令与守卫共享同一误伤面，如 `## API 设计`、`## TODO 清单` 这类正常标题也会被判成疑似外来编号，无预览的一键执行等于吃用户内容） | 打开确认框，逐条列出「现状 → 清理后」对照（`previewForeignNumberingCleanup`，与 `clearForeignNumberingContent`/`stripForeignNumbering` 同一套逻辑，保证预览与实际结果一致）；两个按钮「确认清理」（执行「清理非本插件的标题编号」同一条路径，含 backlink 同步）与「取消」（不改动文件）；点击时该文件已不在任何已打开的标签页 → 提示改为重新打开，不抛错；点击时内容已不含外来编号（用户已自行清理）→ 提示无可清理项，不开确认框 | ✅（1.0.17：`ForeignNumberingCleanupModal`（`src/settings/ForeignNumberingCleanupModal.ts`）+ `createFragment` 构造可点击 Notice + `main.test.ts` 回归 4 例）/ ✅ **手验通过**（2026-08-09 用户真机：Notice 链接可点、确认框「现状→清理后」对照正确、确认后清理生效）|
| **J15** | **`file-open` 的判断依据必须是文件自身内容，不是编辑器缓冲区**（1.0.21，第四轮真机反馈才定位对）：a、b 为正常文件，C 含外来编号——① `a → C`；② `C → b`；③ 编辑器尚未换到位时是否写入；④ 编辑器已换到位时 J9 是否照常 | ①打开 C **立即**弹提示（真机现象是不弹）；②**不**为干净的 b 弹提示（真机现象是弹了、点进去又「没什么可清理」）——两条同源：**编辑器内容正好落后一个文件**，`file-open` 时 `view.file` 已换而缓冲区还显示上一篇；③只判断不写入（宁可这轮不重排，也不能把别的文件的编号写进来）；④内容与文件一致即照常重排 | ✅（1.0.21：`renumberOnOpen` 改用 `vault.cachedRead(file)` 取该文件自身内容做判断——与编辑器换没换到位**完全无关**的时序无关判据；写入侧另加 `editor.getValue() === content` 闸。1.0.20 试过的 `setTimeout(0)` **不够**，实测滞后不止一个事件循环。`main.test.ts` 回归 6 例，**去掉修复后 ①② 两条确实变红**；测试替身补 `vault.cachedRead`——**替身此前让「文件内容」与「编辑器内容」永远相等，正是三轮没测出来的原因**）/ ✅ **2026-08-09 用户真机确认解决**（a→C 弹、C→b 不弹）|
| **J16** | **光标保护精确化**（2026-08-09 用户体验反馈：改标题层级后光标不移开就等不到编号，感觉"卡顿"）：防抖到期时光标所在行新旧内容出现差异——①仅行尾空白不同（J11 原场景，用户刚敲完空格准备继续输入）；②不止行尾空白不同（如敲 `#`/`##` 改了标题层级，前缀随之需要变化） | ①仍原样保留，不吞正在敲的行尾空格（与 J11 一致）；②**不再保护**，本次防抖周期内照常写入新前缀，不必等光标移开、也不必等下一次编辑触发——保护范围从「整行任何差异都冻结直到移开」收窄为「仅行尾空白差异」；快照按实际落盘内容记录、手动命令不受保护等既有行为不变 | ✅（1.0.23 开发中曾用 `levelChangedSinceSnapshot` 借快照判层级；**同版内该判据即被删**——见 J19，改用「只补行尾空白」后层级变化天然立即生效，不需要任何快照推断。`main.test.ts` 保留 2 例作回归护栏）|
| **J17** | **清理外来编号确认框（J14）改为逐条勾选**（2026-08-09 用户体验反馈）：每条候选前加勾选框，默认全部勾选=清理；①取消勾选若干条后点击「确认清理」；②预览的「现状→清理后」对照随勾选状态实时重算；③视觉改为 git 风格 −/+ 两行 diff | ①勾选的标题剥掉外来编号、取消勾选的保留原文——两者在同一次点击、同一事务里立即套用当前模板编号（不必等下一次防抖触发，也不再触发迁移守卫，因为写入后全文必含 WJ）；②每次勾选变化后「清理」态显示"模板前缀+净文本"、「保留」态显示"模板前缀+原文（含外来编号）"，与确认执行逐条一致（`computeForeignCleanupPreview` 预览与确认复用同一份计算）；③−（清理前）/+（清理后）双行对照，卡片式留白、不紧凑；窄屏下勾选框与 diff 块纵向堆叠、可点触区域加大 | ✅（1.0.23：`cleanup.ts` 的 `clearForeignNumberingContent` 加 `keepLines` 选项、`ForeignNumberingPreviewItem` 加 `lineIndex`；`main.ts` 新增 `computeForeignCleanupPreview`/`applyForeignCleanupSelection`；`ForeignNumberingCleanupModal` 重写为逐条勾选 + 实时重算；`main.test.ts` 回归 2 例，含"全勾选一步到位"与"取消勾选后双重编号+守卫不再复发"）/ ✅ **用户真机手验通过**（2026-08-09；唯一反馈是勾选框下方的"清理"文字标签多余，已去掉，改动不涉及逻辑、未 bump）|
| **J18** | **清理外来编号确认框加顶部搜索定位**（2026-08-09 用户体验反馈，候选多时逐条翻找累）：搜索框按标题现状文本（`before`）实时匹配，结果列表逐条展示；①点击某条结果；②搜索框内回车（有匹配时取第一条） | ①②主列表滚动到对应候选（`scrollIntoView({block:"center"})`）并短暂高亮（1.2s 渐隐动画），随后清空搜索框与结果列表；纯定位辅助，**不改变任何勾选状态**；无匹配时结果区显示提示文案，不静默无反应 | ✅（1.0.23：`ForeignNumberingCleanupModal` 加 `searchQuery` 状态 + 搜索框/结果列表渲染，`jumpTo` 复用 `rows: Map<lineIndex, HTMLElement>`；`tsc`/`lint`/`format` 全绿）/ 🔲（DOM 交互留手验——假编辑器环境 `Modal.open()` 空实现，`onOpen`/搜索/滚动/高亮均无法在单测里驱动，同 J17 视觉部分的既有限制）|
| **J19** | **新敲出的标题不编号，除非再按一次 Enter**（1.0.23 真机反馈）：敲 `##` 写完标题文本后，光标**直接移到别的行**——①新标题是否被编号；②新标题末尾带空格时；③写回的变更范围 | ①**本轮防抖就写入编号**，不必再编辑一次。根因：先前的层级判据靠 `headingSnapshots` 逐位对照，而**新增标题会让标题数量相对快照变化**、对照前提被打破而走保守分支整行冻结；解除冻结所依赖的「光标移开后的下一次触发」根本不成立——**移动光标不触发任何事件**（自动路径只挂 `editor-change`）。修法是彻底换掉「整行冻结」这个手段：只把用户刚敲的行尾空白补回去（J11 的原始诉求），编号照常写，不再需要任何快照推断。②编号照写、空格同样保住。③写回**只覆盖真正变化的那一段**（掐掉公共前后缀）——前缀插在行首是纯插入（`from.ch === to.ch === 3`），不再整行替换，光标在行尾不会被 CM6 的位置映射甩走（顺带缩小 J6 暴露面） | ✅（1.0.23：`preserveLine`+`levelChangedSinceSnapshot` 换成 `preserveCursorLineTrailingSpace`；`writeLineDiff` 经新增 `lineChange()` 做最小范围改写，切点避开代理对；`main.test.ts` J19 三例 + J11 五例重写，fuzz 5000×80 全绿）|
| **J20** | **清理外来编号确认框移动端视觉对齐 PC**（2026-08-10 用户实机截图反馈）：J17 原版窄屏（≤480px）把 checkbox 与 diff 从横排切成纵排（checkbox 独占一行、diff 另起一行），用户截图对比 PC 端横排后要求统一——checkbox 悬浮定位对齐 diff 首行（红/before）顶部，diff 占满卡片内容区宽度、不与 checkbox 共享横向空间；红行换行成两排时 checkbox 仍对齐最上方一排；卡片本身透明度不变（用户明确排除） | 移除 `.ah-foreign-guard-item` 的 flex 横/纵排切换，改为 `position:relative` + checkbox `position:absolute`（`top`/`left` 与卡片 `padding-top`/`padding-left` 对齐，天然贴 diff 首行顶部）、diff 块 `width:100%`；桌面与窄屏共用同一套布局（仅窄屏放大 checkbox 可点触区域到 22px 并相应加宽 `padding-left`），不再有断点级的横纵排差异 | ✅（1.0.24：`styles.css` 改写 `.ah-foreign-guard-item`/`.ah-foreign-guard-toggle`/`.ah-foreign-guard-diff` 三处 + 窄屏媒体查询精简；纯 CSS 改动，本机浏览器多宽度截图核验布局符合预期）/ ✅ **2026-08-10 用户真机确认解决**（"观感良好"）|
| **J21** | **快捷键把空标题行转成标题时，编号写入光标错位 + 行尾多余空格**（2026-08-10 用户实机反馈）：用编辑器自带的"设为标题"快捷键（或任何方式）把一个**空行**（或标题文本已被清空的标题行）转成标题，行内容形如 `## `、光标停在行尾（紧跟那个空格），防抖到期后插件写入编号 | 编号写入后光标应落在"编号+间隔符"之后（标题正文起始处，紧接着打字即接上标题文本）；行尾只有**一个**间隔符空格，不重复 | ✅（1.0.25：两处同源根因——① `preserveCursorLineTrailingSpace` 用 `endsWith` 比对新行是否"已带旧行尾空白"，但 `buildPrefix` 在标题为空时以不可见的 WORD_JOINER 收尾，`endsWith` 被这个哨兵挡住误判成"没有"，把旧的那个 `## ` 的空格又补了一份，叠成两个；改为剥掉尾部哨兵后再比对。② `lineChange` 对"旧内容是新内容前缀"（纯追加）算出的插入点与光标恰好同坐标，CM6 对此的默认关联是光标留在插入文本之前，于是编号写完光标反倒卡在数字前；新增 `cursorSelectionForEmptyHeading`，仅当该行本轮标题渲染为空（`text.endsWith(WORD_JOINER)`，与模板前后缀/分隔符风格无关）时随事务显式把光标钉在行尾，已有标题正文的行不受影响、仍交给编辑器自然映射。`main.test.ts` J21 三例）/ ✅ **2026-08-10 用户真机确认解决**（"实测已解决"）|
