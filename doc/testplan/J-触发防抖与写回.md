# J. 触发、防抖与写回 — dev + user

> J1–J5 由 `main.test.ts` 覆盖（经 `obsidian-mock.ts` + vitest 别名加载真实插件，用假编辑器 + 假定时器驱动）。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| J1 | 连续快速输入 | 防抖（默认 300ms）停顿后触发一次 | ✅（0.3.18）|
| J2 | 防抖到期前关闭文件 | 取消待处理更新，不写已关闭文件 | ✅（0.3.18）|
| J3 | 多文件交替编辑 | 防抖以文件为单位，互不取消 | ✅（0.3.18）|
| J4 | 写回用单一事务 | 一次撤销即可回退整次重排 | ✅（0.3.18）|
| J5 | 在设置面板改模板后 | 当前活动文件即时重排 | ✅（0.3.18）|
| J6 | 编号写回不应移动光标/打乱选区 | 光标位置合理保留 | 🔲（假编辑器不建模选区，留手验）|
| J7 | 「立即重新编号」绕过防抖、取消同文件待处理更新 | 立即编号一次；防抖被取消不二次触发；开关关时弹 Notice 不动 | ✅（0.3.18）|
| **J8** | **IME 组合期间防抖顺延**（0.7.17）：拼音组合中防抖到点 | 不写回（避免把组合中的拼音编入标题），顺延一个防抖周期；`compositionend` 后下一周期正常写回 | ✅（`main.test.ts` J8，document 级 compositionstart/end 维护 `imeComposing`）|
| **J9** | **用户需求**：路径规则改投了模板（或模板本身改了样式）后，该路径下**尚未打开过**的文件——只打开、不做任何编辑 | 打开即按当前生效模板自动重排（复用与实时编辑一致的门控：`shouldAutoTrigger` + 按路径解析模板）；已是最新格式的文件打开时静默 no-op（`applyRenumber` 内容未变不写入，不产生多余撤销记录）；全局开关关且非 fm:true，或 fm:false 时打开不触发 | ✅（0.7.24：`file-open` 事件新增 `renumberOnOpen`，`main.test.ts` 回归）|
| **J12** | **迁移守卫误伤**：一个**全文无 WJ、也无任何编号**的文件，某个标题行尾带空白（`## 标题 `，最常见于用户刚敲下空格） | 守卫**不命中**：照常自动编号，不弹「请先清理非本插件的标题编号」 | ✅（1.0.15：比较基准同样去尾空白；根因：迁移守卫拿带行尾归一化的剥离结果与原文直接比，空白差异被当成「剥掉了编号」）|
| **J11** | **打字被打断**：防抖到期时光标正停在某个**将被改写**的行上——最常见的是在标题文本末尾敲了空格、正准备继续输入 | **只把用户刚敲的行尾空白补回本轮结果**，编号照常写入；光标不动连跑两轮时第二轮不发空事务；光标移开后那一轮空白按规则清掉；补回的空白不产生幻影改名。**手动命令**（立即重新编号 / 打开文件即重排 / 改模板即时重排）不受保护，行尾空白照常归一化 | ✅（1.0.23 定案，机制见 J19；`main.test.ts` 5 例；根因：自动路径没有光标守卫，防抖到期整行重写，行尾空白被剥离器的归一化吞掉）|
| **J10** | **迁移体验**：从其他编号插件 / 手写编号迁移过来的文件（全文无 WJ）第一次被自动路径（编辑防抖 / 打开文件 / 改模板后即时重排）碰到，且标题被判定为「像外来编号」（如 `## 1 红米`） | **跳过本次自动写入**（否则会叠成 `## 1 1 红米`），改为可点击的 Notice 提示（提示时机见 J13）；**手动命令**（立即重新编号 / 清除编号 / 清理外来编号）不受此守卫影响，照常执行 | ✅（1.0.7：`hasUnclaimedForeignNumbering`，`cleanup.test.ts` + `main.test.ts` 回归）|
| **J13** | **迁移守卫提示只为「用户当前正看着的文件」发声**：① 在 a.md（外来编号）里打字后、防抖到期**前**切到 b.md；② 改模板触发批量刷新（含后台打开的疑似文件）；③ 同一文件里连续打字、防抖反复到期；④ 切到别的文件；⑤ 切走再切回；⑥ 文件被清理干净 | ①**不**弹提示（用户眼前是 b.md，弹了会被读成在说 b.md）；②只有当前活动的那个疑似文件弹；③屏幕上**始终只有一条**，不堆叠不闪烁；④上一条被**主动收起**，不留孤儿提示；⑤重新提示；⑥主动收起 | ✅（1.0.19：`showForeignNumberingGuardNotice` 按活动文件统一裁决；`main.test.ts` 6 例；1.0.21 真机确认；根因：守卫检查的发起方不保证是用户正看着的文件）|
| **J14** | **迁移守卫 Notice 可点击 → 清理预览确认框**：点击 J10 命中后弹出的 Notice 上的可点击文案（不做无预览的一键清理——`## API 设计` 这类正常标题也可能被判成疑似外来编号） | 打开确认框，逐条列出「现状 → 清理后」对照（与实际清理同一套逻辑）；「确认清理」走「清理非本插件的标题编号」同一路径（含 backlink 同步），「取消」不改文件；点击时文件已不在任何标签页 → 提示重新打开；内容已不含外来编号 → 提示无可清理项，不开确认框 | ✅（1.0.17：`ForeignNumberingCleanupModal`，`main.test.ts` 4 例；2026-08-09 真机手验通过）|
| **J15** | **`file-open` 按文件自身内容判断，不看编辑器缓冲区**：a、b 为正常文件，C 含外来编号——① `a → C`；② `C → b`；③ 编辑器尚未换到位时；④ 编辑器已换到位时 | ①打开 C **立即**弹提示；②**不**为干净的 b 弹提示；③只判断不写入（宁可这轮不重排，也不把别的文件的编号写进来）；④内容与文件一致即照常重排（J9） | ✅（1.0.21：`renumberOnOpen` 用 `vault.cachedRead` 取文件自身内容判断，写入前核对编辑器内容；`main.test.ts` 6 例；2026-08-09 真机确认；根因：`file-open` 时编辑器缓冲区可能还停在上一篇）|
| **J16** | **光标保护只护行尾空白**：防抖到期时光标所在行新旧内容出现差异——①仅行尾空白不同（J11 原场景）；②不止行尾空白不同（如敲 `#`/`##` 改了标题层级，前缀随之需要变化） | ①原样保留，不吞正在敲的行尾空格；②**不再保护**，本次防抖周期内照常写入新前缀，不必等光标移开 | ✅（1.0.23，由 J19 的「只补行尾空白」天然满足；`main.test.ts` 2 例）|
| **J17** | **清理外来编号确认框逐条勾选**：每条候选前加勾选框，默认全部勾选=清理；①取消勾选若干条后点击「确认清理」；②预览对照随勾选状态实时重算；③视觉为 git 风格 −/+ 两行 diff | ①勾选的剥掉外来编号、未勾选的保留原文，同一次点击、同一事务里立即套用当前模板编号，之后不再触发迁移守卫；②「清理」态显示「模板前缀 + 净文本」、「保留」态显示「模板前缀 + 原文」，与确认执行逐条一致；③−/+ 双行对照；窄屏可点触区域加大 | ✅（1.0.23：`main.test.ts` 2 例；2026-08-09 真机手验通过）|
| **J18** | **清理外来编号确认框顶部搜索定位**：搜索框按标题现状文本实时匹配、逐条展示结果；①点击某条结果；②搜索框内回车（有匹配时取第一条） | ①②主列表滚动到对应候选并短暂高亮，随后清空搜索；**不改变任何勾选状态**；无匹配时显示提示文案 | ✅ 逻辑（1.0.23）/ 🔲 DOM 交互手验（单测驱动不了 Modal）|
| **J19** | **新敲出的标题当轮就编号**：敲 `##` 写完标题文本后，光标**直接移到别的行**——①新标题是否被编号；②新标题末尾带空格时；③写回的变更范围 | ①**本轮防抖就写入编号**，不必再编辑一次；②编号照写、行尾空格同样保住；③写回**只覆盖真正变化的那一段**（前缀插在行首是纯插入，光标不被甩走） | ✅（1.0.23：`preserveCursorLineTrailingSpace` + `writeLineDiff` 最小范围改写，设计见 spec §3.19；`main.test.ts` J19 三例；根因：旧的「整行冻结」要等「光标移开后的下一次触发」解冻，而移动光标不触发任何事件）|
| **J20** | **清理外来编号确认框：窄屏与 PC 同布局**：在窄屏（≤480px）打开确认框，候选的 diff 折成两行 | 勾选框对齐 diff 首行顶部，diff 占满卡片宽度；窄屏与桌面同一套布局，仅放大勾选框的可点触区域 | ✅（1.0.24，纯 CSS；2026-08-10 真机确认）|
| **J21** | **快捷键把空标题行转成标题**：把空行（或标题文本已清空的标题行）转成标题，行内容形如 `## `、光标停在行尾，防抖到期后插件写入编号 | 光标落在「编号 + 间隔符」之后（紧接着打字即接上标题文本）；行尾只有**一个**间隔符空格 | ✅（1.0.25：`main.test.ts` J21 三例；2026-08-10 真机确认；根因两处：空标题的编号以 WJ 收尾，行尾空白比对被它挡住而多补一个空格；纯追加时光标默认留在插入点之前——修为剥哨兵后比对 + `cursorSelectionForEmptyHeading` 钉光标）|
