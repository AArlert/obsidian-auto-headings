# K. 路径规则解析（M5） — dev + user

> `pathrules.test.ts`（纯函数）+ `main.test.ts`（`getTemplateForFile` 接线）覆盖。GUI 路径规则表（增删/拖拽/模板下拉/补全/兜底提示）属 DOM 层，留 user_tests 手验。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| K1 | `/` 根规则 + 任意文件 | 命中根规则，用其模板 | ✅ |
| K2 | 文件夹规则 `Projects/` + `Projects/a.md` | 命中（含深层 `Projects/sub/b.md`）；`Other/a.md` 不命中 | ✅ |
| K3 | 文件规则 `读书笔记/深度工作.md` | 仅精确匹配该文件 | ✅ |
| K4 | 多规则：文件 ＞ 最长文件夹前缀 ＞ `/` 根 | 取最具体者 | ✅ |
| K5 | 具体度并列（两条**不同**文件夹名恰好等长，如 `Ab/` 与 `Cd/`） | 无优劣可分，退化为「列表靠后者胜出」的确定性兜底 | ✅ |
| K6 | 无任何规则命中 | 返回 null → 自动静默 / 手动 Notice（见 I7）| ✅ |
| K7 | 归一化：反斜杠/前导斜杠/重复斜杠 | 等价匹配 | ✅ |
| K8 | 重命名模板 | 引用该模板的路径规则自动改名（`renameTemplate` 同步 `data.json`）| ✅（手验）|
| K9 | 删除被规则引用的模板 | 弹「知情确认」对话框，可降级/改投/连规则删 | ✅（手验，`DeleteTemplateModal`）|
| K10 | 无 `/` 根规则且全局自动编号=开 | 面板顶部显示兜底缺失提示条 | ✅（手验，`hasRootRule` 已接线）|
| **K11** | **用户报告 bug**：[+ 添加规则] 新增一行（路径未填，`pattern: ""`）后先选模板，尚未输入路径 | 该未配置行**不应匹配任何文件**，不应把新模板套用到当前文件并触发编号；也不应被 `hasRootRule` 误判为已有根规则 | ✅（0.7.22：`normalizePattern` 曾把空串折算为根 `/`，导致新增行未填路径即先当根规则命中全部文件；改为空串单独返回 `""`、`ruleMatches` 对其恒不匹配，`pathrules.test.ts` 回归）|
| **K12** | **用户报告 bug**：新建第二条规则，路径也填 `/`（与已有根规则重复），投给另一个模板 | 提交路径时（失焦）**阻断保存**：路径回退为改前的值，弹 Notice 提示「已被第 N 条规则占用」；不产生「靠哪条更靠后决定生效」的隐式覆盖。文件夹/文件路径重复同理阻断，不止根 `/` | ✅（0.7.23：`findDuplicatePatternIndex` 纯函数 + `PathRules.ts` `commitPattern` 接线，`pathrules.test.ts` 回归）/ 🔲 手验 DOM（Notice 文案与阻断手感）|
| **K13** | **用户报告 bug**：给某文件夹路径投新模板（如 `新路径`，漏打尾斜杠），该路径下已按旧规则编号过的文件重新打开 | 用户填 `新路径`（无尾斜杠）被当成对不存在文件的**精确匹配**文件规则，不命中该文件夹下任何文件，行为上像「改了模板没生效」——**根因是文件夹/文件规则的区分系于尾斜杠、手动输入易漏打**，而非 `renumberOnOpen`（J9）机制本身有问题（复现已确认：正确加上 `/` 后重排立即生效）。**修复**：① 路径输入接自绘建议弹窗（`PathSuggest.ts`，参考 numeroflip/obsidian-auto-template-trigger），模糊匹配 vault 内全部文件夹/文件、按命中位置+文件夹优先排序，选中文件夹自动带尾斜杠；② 手动输入未选建议项时，`autocompleteFolderSlash`（`pathrules.ts`）在提交时兜底：输入若与某个真实存在的文件夹路径精确相等但缺尾斜杠，自动补全 | ✅（1.0.4：`pathrules.test.ts` 覆盖 `filterPathCandidates`/`autocompleteFolderSlash` 纯函数）/ ✅（1.0.5：用户实测反馈「点击输入框不弹出下拉」，根因是弹窗 z-index 用了 `--layer-popover`（低于「设置」模态框的 `--layer-modal`）被盖住不可见——键盘 Enter 选中不依赖可见性，故此前只发现「打字+回车能选、点击看不到」，改 `--layer-menu` 修复）/ 🔲 手验 DOM（弹窗现在的可见性/排序/键盘选择手感，留用户再次实测确认）|
| **K14** | **用户报告疑似 bug**：某行路径已提交为 `/`（根规则）后再次点击该输入框，下方建议列表出现「又一个 `/`／内容诡异」的观感 | 复现（`repro.mjs` 纯函数实测）：`collectPathCandidates` 手动注入的根候选 `{path:"",isFolder:true}` 一旦 needle 恰为 `/`（`"".indexOf("/")` 恒 `-1`）就被自身逻辑排除，`filterPathCandidates` 转而对**字面**含 `/` 的候选做子串匹配——顶层文件夹全部消失、只剩深层嵌套项，观感诡异。**调研**：直接读取参考实现 numeroflip/obsidian-auto-template-trigger 源码（`fileSuggest.ts` `FolderSuggest.getSuggestions`）确认其 `.contains(inputStr)` 为扁平模糊匹配、且用 `folder.path &&` **显式排除根目录**，不做分层浏览、选中即刻 `close()`——本插件此前的“分层 datalist”改扁平弹窗是有意为之（K13），本次不应走回头路。**修复**（与用户确认采用「点文字＝选中、文件夹行小箭头＝下钻」的方案，贴合参考实现「点击即选中」的默认手感，同时满足用户「空输入时按层级浏览」的诉求）：① `collectPathCandidates` 不再注入合成根候选（对齐参考实现）；② `filterPathCandidates` 的 needle 剥离前导 `/`（`normalizePattern` 本就视其为根锚点写法，不是字面字符）；③ 输入框为空时弹窗进入「分层浏览」：顶部 header 显示当前层路径且可点击选中该层（含根 `/`）、非根层多一个 `⬅` 返回上一级；子项行按「文件夹优先、字典序」列出，行文字点击＝选中（与打字搜索模式手感一致），文件夹行独立小箭头 `▸`＝下钻查看下一层（不选中）；一旦开始打字则退出浏览、回到既有全库模糊搜索（不变） | ✅（1.0.11：`parentDir`/`listImmediateChildren`/`filterPathCandidates` 前导斜杠场景 `pathrules.test.ts` 回归 38 例全过）/ ✅ 手验 DOM（2026-07-18 用户真机实测「效果达标」；1.0.13 随手感反馈把 `⬅`/`▸` 统一为 lucide `arrow-left`/`arrow-right` 同族图标）|
| **K14b** | **用户报告（1.0.11 实测反馈）**：新增空行进分层浏览外观正确，但**把某行配好 `/` 或 `A/` 后再次点击该行**，弹窗又回落成「匹配一堆」的扁平列表——视觉设计与功能设计不统一 | 根因：1.0.11 的模式判断只看「输入框是否为空」，配好的行 value 非空 ⇒ 走扁平搜索分支（`/` 经前导斜杠剥离后 needle 为空 ⇒ 返回全部候选，`A/` ⇒ 匹配一堆含 `a/` 的项）。**修复**：模式判断改由 `browseDirForInput(value, folderPaths)` 决定——空 / 根 `/` / **真实存在的文件夹（尾斜杠）** 一律进分层浏览（`A/` 浏览**进** A，header 显示当前层、可 `⬅` 返回上一级），与新增空行外观一致；只有正在打字的片段（`Pro`）、文件规则（`A/note.md` 无尾斜杠）、尚不存在的文件夹名才交给扁平模糊搜索 | ✅（1.0.12：`browseDirForInput` 纯函数 `pathrules.test.ts` 回归）/ ✅ 手验 DOM（2026-07-18 用户真机实测确认「配好 `/` 再点击仍是分层浏览」，效果达标）|
| **K15** | **M12「不编号」伪模板**：某文件夹规则（如 `daily/`）的模板下拉选「不编号」，该文件夹下文件被编辑 / 打开 / 跑「立即重新编号」命令 | 自动路径：命中「不编号」规则的文件**静默不编号**（更泛的根规则不兜底——伪模板参与具体度解析并胜出）；已有编号**冻结不动**（与 frontmatter `false` 同语义，想去掉编号用清除命令）；手动「立即重新编号」弹专用 Notice「该路径已设为不编号」而非误导性的「未匹配任何路径规则」；模板删除/失效逻辑不把「不编号」当失效模板 | ✅（1.0.13：`getTemplateForFile` 哨兵 `$none` 返回无模板 + `resolvesToNoNumbering` 专用 Notice + `TemplateStore.rename` 拒占哨兵名 + `main.test.ts` 回归 3 例）/ 🔲 手验 DOM（下拉伪选项与批量按钮置灰观感）|
| **K16** | **M12 多文件批量重编号**：路径规则行内点批量按钮 → 确认对话框（显示命中文件数）→ 确认 | 对该规则**路径模式命中**的全部 Markdown 文件重编号，每个文件用**它自己解析出的最具体规则**的模板（与自动路径一致，不会把父规则模板强加给已被子规则接管的文件）；跳过：frontmatter `false`、含未接管外来编号（J10 守卫同源）、解析为「不编号」/无可用模板；**已打开的文件走编辑器单一事务**（可撤销、无 vault.process 竞态），未打开的走 `vault.process`；backlink 同步照常（同文件内链折进主写回、跨文件汇总一条 Notice）；完成 Notice 汇总「改写 X / 无变化 Y / 跳过 Z」；命中 0 个文件时不弹对话框、直接 Notice；「不编号」规则行的批量按钮置灰 | ✅（1.0.13：`batchRenumberRule` 编辑器/vault 双通道 + backlink 改写数汇总一条 Notice（`syncBacklinksCounted` 拆分）+ 行内按钮 + 确认 Modal + `main.test.ts` 回归 5 例）/ 🔲 手验 DOM（确认框文案、按钮观感、批量后打开文件的实际编号）|
