# K. 路径规则解析（M5） — dev + user

> `pathrules.test.ts`（纯函数）+ `main.test.ts`（`getTemplateForFile` 接线）覆盖。GUI 路径规则表（增删/拖拽/模板下拉/补全/兜底提示）属 DOM 层，留 user_tests 手验。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| K1 | `/` 根规则 + 任意文件 | 命中根规则，用其模板 | ✅ |
| K2 | 文件夹规则 `Projects/` + `Projects/a.md` | 命中（含深层 `Projects/sub/b.md`）；`Other/a.md` 不命中 | ✅ |
| K3 | 文件规则 `读书笔记/深度工作.md` | 仅精确匹配该文件 | ✅ |
| K4 | 多规则：文件 ＞ 最长文件夹前缀 ＞ `/` 根 | 取最具体者 | ✅ |
| K5 | 具体度并列（两条**不同**文件夹名恰好等长，如 `Ab/` 与 `Cd/`） | 无优劣可分，退化为「列表靠后者胜出」的确定性兜底 | ✅ |
| K6 | 无任何规则命中 | 返回 null → 自动静默 / 手动 Notice（见 I7）| ✅ |
| K7 | 归一化：反斜杠/前导斜杠/重复斜杠 | 等价匹配 | ✅ |
| K8 | 重命名模板 | 引用该模板的路径规则自动改名（`renameTemplate` 同步 `data.json`）| ✅（手验）|
| K9 | 删除被规则引用的模板 | 弹「知情确认」对话框，可降级/改投/连规则删 | ✅（手验，`DeleteTemplateModal`）|
| K10 | 无 `/` 根规则且全局自动编号=开 | 面板顶部显示兜底缺失提示条 | ✅（手验，`hasRootRule` 已接线）|
| **K11** | **用户报告 bug**：[+ 添加规则] 新增一行（路径未填，`pattern: ""`）后先选模板，尚未输入路径 | 该未配置行**不应匹配任何文件**，不应把新模板套用到当前文件并触发编号；也不应被 `hasRootRule` 误判为已有根规则 | ✅（0.7.22：`normalizePattern` 曾把空串折算为根 `/`，导致新增行未填路径即先当根规则命中全部文件；改为空串单独返回 `""`、`ruleMatches` 对其恒不匹配，`pathrules.test.ts` 回归）|
| **K12** | **用户报告 bug**：新建第二条规则，路径也填 `/`（与已有根规则重复），投给另一个模板 | 提交路径时（失焦）**阻断保存**：路径回退为改前的值，弹 Notice 提示「已被第 N 条规则占用」；不产生「靠哪条更靠后决定生效」的隐式覆盖。文件夹/文件路径重复同理阻断，不止根 `/` | ✅（0.7.23：`findDuplicatePatternIndex` 纯函数 + `PathRules.ts` `commitPattern` 接线，`pathrules.test.ts` 回归）/ 🔲 手验 DOM（Notice 文案与阻断手感）|
| **K13** | 给某文件夹路径投新模板时漏打尾斜杠（如 `新路径`），该路径下已编号的文件重新打开 | 路径输入有建议弹窗（模糊匹配全库文件夹 / 文件，选中文件夹自动带尾斜杠）；手动输入未选建议项时，若与某个真实文件夹精确相等但缺尾斜杠，提交时自动补全——新模板对该文件夹生效（设计见 spec §3.8） | ✅（1.0.4：`pathrules.test.ts` 覆盖 `filterPathCandidates` / `autocompleteFolderSlash`；1.0.5 修弹窗被设置模态框遮住；根因：文件夹 / 文件规则的区分系于尾斜杠，手动输入易漏打）/ 🔲 手验 DOM（弹窗可见性、排序、键盘选择手感）|
| **K14** | 某行路径已提交为 `/`（根规则）后再次点击该输入框；或输入框为空时打开建议弹窗 | 进入「分层浏览」：顶部显示当前层路径、可点击选中该层（含根 `/`），非根层可返回上一级；子项文件夹优先、字典序；**点行文字＝选中，文件夹行的小箭头＝下钻**；一开始打字即回到全库模糊搜索（设计见 spec §3.8） | ✅（1.0.11：`pathrules.test.ts` 38 例；2026-07-18 真机确认；根因：根候选被自身逻辑排除、`/` 被当字面字符做子串匹配，顶层文件夹全部消失）|
| **K14b** | 把某行配好 `/` 或 `A/` 后再次点击该行 | 与新增空行一致进入分层浏览（`A/` 浏览**进** A）；只有正在打字的片段、文件规则（无尾斜杠）、尚不存在的文件夹名才走扁平模糊搜索 | ✅（1.0.12：`browseDirForInput` 纯函数 `pathrules.test.ts` 回归；2026-07-18 真机确认；根因：模式判断只看输入框是否为空）|
| **K15** | **「不编号」伪模板**：某文件夹规则（如 `daily/`）的模板下拉选「不编号」，该文件夹下文件被编辑 / 打开 / 跑「立即重新编号」命令 | 自动路径：命中「不编号」规则的文件**静默不编号**（更泛的根规则不兜底——伪模板参与具体度解析并胜出）；已有编号**冻结不动**（想去掉编号用清除命令）；手动「立即重新编号」弹专用 Notice「该路径已设为不编号」；删模板 / 失效逻辑不把「不编号」当失效模板（设计见 spec §3.8） | ✅（1.0.13：`main.test.ts` 回归 3 例）/ 🔲 手验 DOM（下拉伪选项与批量按钮置灰观感）|
| **K16** | **多文件批量重编号**：路径规则行内点批量按钮 → 确认对话框（显示命中文件数）→ 确认 | 对该规则路径命中的全部 Markdown 文件重编号，每个文件用**它自己解析出的最具体规则**的模板；跳过 frontmatter `false`、含未接管外来编号（同 J10 守卫）、解析为「不编号」/ 无可用模板的；已打开的文件走编辑器单一事务（可撤销），未打开的走 `vault.process`；backlink 同步照常、跨文件汇总一条 Notice；完成 Notice 汇总「改写 X / 无变化 Y / 跳过 Z」；命中 0 个文件时不弹对话框；「不编号」规则行的批量按钮置灰（设计见 spec §3.8） | ✅（1.0.13：`main.test.ts` 回归 5 例）/ 🔲 手验 DOM（确认框文案、按钮观感、批量后打开文件的实际编号）|
