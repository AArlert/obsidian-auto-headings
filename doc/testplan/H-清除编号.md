# H. 清除编号（M6） — dev + user

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| H1 | 「清除当前文件编号」对已编号文件 | 所有前缀剥成裸标题 | ✅（0.6.0 `cleanup.test.ts`）|
| H2 | 清除时文件已无任何模板命中 | 仍用「全样式并集」剥离器尽力剥净 | ✅（0.6.0）|
| H3 | 清除含 cjk/circled/alpha 混合历史前缀的文件 | 全部样式都能剥 | ✅（0.6.0）|
| H4 | 清除后立即「立即重新编号」 | 回到正确单层编号（往返幂等） | ✅（0.6.0）|
| H5 | ⚠️ 用户手写 `1.1 标题` 被误剥 | 接受（与 stripPrefix 同源风险，spec §3.10）| ⚠️ |
| **H6** | 清除全库时有文件正被编辑（防抖挂起） | 清除期间全局自动编号被临时压制、待处理防抖被取消——刚清掉的编号**不会**被立刻编回去；完毕自动恢复 | ✅（0.7.11，`main.test.ts`；spec §3.13）|
| **H7** | **清除全库前先持久关闭「全局自动编号」**（0.7.17）：确认清库 | `settings.autoNumber` 落盘置 false 后再清——否则清完开关还开着、一编辑又被编回去（「全开着却没一个被编号」反直觉）；需要时手动再开；面板开着时开关状态即时刷新 | ✅（`main.test.ts` H7；双语 desc 与确认对话框已注明）|
| **H8** | 清除全库 / 固化（全库）时某文件正被打开、编辑器里有未落盘的改动 | 已打开的文件在它的编辑器里以事务改写（不覆盖未落盘改动，可在该编辑器里撤销），未打开的走 `vault.process` 原子读改写（设计见 spec §3.10） | ✅（1.2.0，`main.test.ts` H8 两例；根因：原先逐文件读盘改写，可能覆盖未保存的编辑）|
| **H9** | **固化编号并交还所有权（全库）**（M12）：对含编号标题 + 内链的库执行 | 编号**保留为普通文本**；全库 U+2060 归零（标题行与 `[[file#锚点]]` **同时**剥净，故内链仍解析得到）；`settings.retired` 落盘 true | ✅（1.0.14） |
| **H10** | 固化后编辑任意文件（含 frontmatter `obsidian-auto-headings: true` 的文件） | **一律不自动编号**——`retired` 闸在 `shouldAutoTrigger` 首行、凌驾于 `fm:true`；否则固化下来的普通文本编号会被叠上新前缀成双重编号 | ✅（1.0.14） |
| **H11** | 固化后解除 retired（面板「恢复接管」）→ 编辑固化过的文件 | `guardForeignNumbering` 命中（固化编号此时已是「外来编号」）→ 每文件每会话提示一次并跳过自动写入；跑「清理非本插件的标题编号」后可干净重编 | ✅（1.0.14） |
| **H12** | 固化命令的确认对话框与离场状态可见性 | 对话框点明五件事（编号保留 / 全库 WJ 一并移除含链接锚点 / 不在撤销历史建议备份 / 停止一切自动编号 / 恢复接管需先清外来编号）；`retired=true` 时全局设置页显示提示条 + 「恢复接管」按钮 | 🔲（GUI 手验）|
| **H13** | **「清除当前文件编号」要有持久效果**：对一个**仍会被自动重编号**的文件（全局开关开或 `fm: true`，且命中模板）执行清除 | 清除的同时写入 frontmatter `obsidian-auto-headings: false`，**并入同一事务**（一次撤销即整体回退）；此后编辑不再被立刻编回去 | ✅（1.0.15，设计见 spec §3.19；根因：清除只取消了当次防抖计时器，下一次按键即重新编号）|
| **H14** | 清除时该文件**本就不会**被自动重编号（全局关且非 `fm: true`，或路径规则解析为「不编号」） | 只清除，**不写** frontmatter——不为一个不存在的问题往用户文件里塞属性 | ✅（1.0.15）|
| **H15** | 清除（已暂停）→ 跑「立即重新编号」 | 该命令顺带移除 `obsidian-auto-headings: false`、恢复自动接管，构成 清除↔重新编号 的对称闭环；若该键是 frontmatter 里**唯一**一项，整个 `---` 块一并移除，不留空块 | ✅（1.0.15）|
| **H16** | 清除并暂停时 frontmatter 的三种形态：① 已有块且含其他键 ② 完全没有 frontmatter ③ 已经是 `false` | 分别为：在既有块内插入一行 / 新建 `---` 块置于文件最前 / 识别为已暂停不重复写。未闭合的 frontmatter 视为畸形，**不改**（保守跳过） | ✅（1.0.15）|
| **H17** | 清除全库时，别的笔记里有指向带编号标题的链接 `[[a#⁠1 ⁠概述]]`，本笔记里有自链接 `[[#⁠1 ⁠概述]]` | 「改名不断链」开：两处都同步成清除后的锚点（`[[a#概述]]` / `[[#概述]]`），清完只弹一次链接合计提示；关：链接不动（与其他改写路径一致） | ✅（1.2.0，`main.test.ts` H17 两例；根因：原先清全库只刷新快照、不同步链接，清完这些链接全断）|
| **H18** | Backlink 同步的引用方**正被打开**：典型是批量改写（清除全库 / 模式切换清除 / 批量重编号）里 A、B 互相引用且都开着——先改写了 A（尚未落盘），再因 B 的标题变化去同步 A 里指向 B 的链接 | 同步写进已打开的引用方时走它的编辑器，不读盘改写（否则会用磁盘旧内容冲掉刚完成的改写）；未打开的引用方照旧 `vault.process` | ✅（1.2.0，`main.test.ts` H18 两例；真机：两篇分屏，刚粘贴未落盘的链接随另一篇改名同步、粘贴内容不丢）|
| **H19** | 固化（全库）改走批量通道后的语义不回归 | 仍是**全文**剥 WJ（标题与链接锚点两侧同时归零，H9）；`retired` 先落盘再动文件；结束后快照清空、仅显示文件的编号随之消失 | ✅（1.2.0，`main.test.ts` H19 三例：已打开文件的编辑器分支标题+链接锚点 WJ 一并剥净、快照清空、retired 在任何 `vault.process` 写入前已落盘；H9 既有例覆盖未打开文件分支）|
