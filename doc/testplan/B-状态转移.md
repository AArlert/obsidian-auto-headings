# B. 状态转移：改"格式字段"后再触发 — dev（重点！）★

> 基线：先用「配置 X」对 `## 标题`（含 `### 子`）编号，改某字段为 Y，**再次触发**，断言无叠加。

| ID | 起始（已编号） | 改动 | 预期（再触发后） | 状态 |
|----|----------------|------|-----------------|------|
| **B1** | cjk + 标题间隔符=空格 → `## 一 标题` | 间隔符 空格 → `、` | `## 一、标题` | ✅（0.3.16 容差分隔符）|
| **B2** | arabic + 空前缀 → `## 1 标题` | 前缀 `（空）` → `第` | `## 第1 标题` | ✅（0.3.18 方案A）|
| **B3** | arabic + 无后缀 → `## 1 标题` | 后缀 `（空）` → `章` | `## 1章 标题` | ✅（0.3.18 方案A）|
| **B4** | arabic `.` → `### 1.1 子` | 序号间隔符 `.` → `-` | `### 1-1 子` | ✅（0.3.16）|
| **B5** | arabic 标题间隔符=`. ` → `## 1. 标题` | 间隔符 `. ` → 空格 | `## 1 标题` | ✅（0.3.16）|
| B6 | arabic → `## 1 标题` | 样式 arabic → cjk | `## 一 标题` | ✅ |
| B7 | cjk → `### 一.一 子` | 样式 cjk → arabic | `### 1.1 子` | ✅ |
| B8 | 前缀「第」+后缀「章」，`inherit` 在父子级间翻转 | 父子重新拼接、无残留叠加、幂等 | `第1.1章 子`↔`第1章 子` 双向均剥净、幂等 | ✅（0.6.2 UVM 20000×80 全绿）|
| B9 | 任意字段连改两轮（前缀+后缀+间隔符同改） | 只剩一层正确前缀 | 不叠加 | ✅（0.3.18）|
| **B10** | titleSeparator=`。`，标题以数字起头 `2024 总结` | 编号且**保留** `2024` | `## 1。2024 总结` | ✅（0.6.3 `tolerantInnerSeparator`，见 U2）|

> **B2/B3/B9（0.3.18，方案A）**：`stripPrefix` 用 `affixAlternation` 接受「当前值 + 空串 + 全模板前后缀并集」候选，改前缀/后缀后不再叠加。剥离同时改为**只剥一层**（见 §1「2024 折中」）。
>
> **B1/B4/B5（0.3.16）**：`tolerantSeparator` 容差匹配历史分隔符，分隔符变更后不叠加。
