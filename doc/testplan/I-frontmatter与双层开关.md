# I. frontmatter / 双层开关 — dev + user

> I1/I2/I4/I5 由 `main.test.ts` + `frontmatter.test.ts` 覆盖；I3/I6/I7 依赖 M5（路径规则），于 0.5.0 落地。0.6.1 起属性值改为布尔复选框（`true`/`false`）。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| I1 | 全局自动编号=开 + 无 frontmatter | 自动触发 | ✅（0.3.18）|
| I2 | 全局=开 + `fm: false` | 不自动触发；已有编号冻结 | ✅（0.6.1）|
| I3 | 全局=关 + `fm: true` | **仍**自动触发（文件级强制 opt-in） | ✅（0.6.1）|
| I4 | 全局=关 + 无 fm / `fm: false` | 不触发 | ✅（0.3.18）|
| I5 | fm 非法值（旧版 `ON`/`OFF`、大小写不对） | 忽略该键，按全局默认 | ✅（0.6.1）|
| I6 | 「立即重新编号」遇 `fm: false` 或全局=关 | 照常编号（手动命令绕过开关）；**1.0.15 起顺带移除 `fm: false`**、恢复自动接管（与 H13 的暂停构成闭环，见 H15） | ✅（0.6.1；1.0.15 改）|
| I7 | 无任何路径规则命中：自动触发 / 手动命令 | 自动静默跳过 / 手动弹 Notice | ✅（0.5.0）|
| **I8** | **`fm: false` 的文件里改标题文本**（纯改名，不涉及编号），`updateBacklinks` 开 | **仍**同步别处指向它的内链（1.0.15 起放宽）——backlink 同步是全局能力，与「该文件不自动编号」正交；`shouldBacklinkStandaloneTrigger` 不再因 `fm: false` 退出。**这是对已上架语义的有意收窄**：`fm: false` 从「该文件完全不被处理」改为「该文件不自动编号」，因为 H13 起它同时承担「清除后暂停」的职责，若连改名不断链也停掉，等于用清除编号换掉了插件的第一价值 | ✅（1.0.15）|
