# V. 虚拟编号模式（M14，1.2.0） — dev + user

> 新功能（规格见 spec.md [3.22](../spec/3.22-虚拟编号模式.md)，排期见 [Roadmap M14](../spec/5-Roadmap.md#milestone-14--虚拟编号模式仅显示不写文件)）：
> 路径规则可单独选「写入文件」或「仅显示」；仅显示时编号只在编辑视图（CM6 装饰）与阅读视图（post-processor）
> 里渲染，永不写入文件。模型 / 门控 / 清除 / 切换筛选走 dev 单测（`virtual.test.ts`、`settings` / `pathrules` /
> `cleanup` / `main` 追加、UVM 新 oracle）；渲染的位置映射在 node 环境直接测 CM6 状态；视觉、输入法、PDF、
> 移动端、性能留真机手验。

| ID | 操作 | 预期 | 状态 |
|----|------|------|------|
| V1 | 全新库首次启用插件（无 data.json、无 `templates/` 目录） | 根规则 `/` 的模式为「仅显示」；打开带标题的笔记能看到编号，文件字节不变 | ✅ 判据（`settings.test`）；显示待周期 2 |
| V2 | 已有 `templates/` 目录但没有 data.json 的库（老用户从未改过设置）升级到 1.2.0 | 视为升级：模式保持「写入」，行为与 1.1.4 一致 | ✅ |
| V3 | 老用户（有 data.json、路径规则无 `mode` 字段）升级 | 所有规则按「写入」处理，零迁移；data.json 里非法的 `mode` 值归一化为「写入」 | ✅ |
| V4 | 新装分支加载后不改任何设置 | 不调用 `saveData`（data.json 不落盘），等用户第一次改设置时才写 | ✅ |
| V5 | 单测锁顺序：onload 中 `loadSettings` 在 `templateStore.init()` 之前执行 | 顺序被调换时测试失败 | ✅ |
| V6 | 多设备同步：设备 B 在 data.json 同步到位之前加载插件（被误判为新装），随后 data.json 被同步改写 | 触发 `onExternalSettingsChange` 重新载入，设备 B 回到写入模式；设备 A 的设置不被改成「仅显示」 | ✅ 逻辑；真机同步待手验 |
| V7 | `resolveNumberingMode`：嵌套规则 `/`（仅显示）与 `/Papers`（写入），分别查 `/Papers/a.md`、`/Notes/b.md` | 前者写入、后者仅显示；具体度与 `resolvePathRule` 完全一致；「不编号」规则返回 null | ✅ |
| V8 | 仅显示文件里任意编辑（改标题、增删标题、改层级、粘贴多行） | 文件字节不变（UVM oracle：任意操作序列下虚拟文件字节不变）；编号显示随之更新 | ✅ 不写（`main.test` + UVM 虚拟记分板）；显示待周期 2 |
| V9 | 仅显示文件里有白名单标题、`<!-- skip -->` 标题、超出层级范围的标题 | 这些标题不显示编号且不推进计数器，与写入模式的编号结果逐行一致 | ✅ |
| V10 | 仅显示文件：frontmatter `false` / 全局自动编号关闭 / 清库进行中 / 有效规则为「不编号」 | 都不渲染编号 | ✅ |
| V11 | 有效规则为「不编号」的文件，frontmatter 设为 `true` | 不写也不显示（「不编号」下没有可用模板，与写入模式一致；周期 1 订正） | ✅ |
| V12 | 仅显示文件里过半标题带手写编号（如 `## 1. 引言` / `## 2. 方法`）；另测只有个别 `## 2024 总结` | 前者不渲染、打开时弹仅显示版提示，点开清理只剥手写编号不写插件编号；后者照常编号（真机实测后改为过半判定） | ✅ 单测 + 真机 |
| V13 | 仅显示文件里残留插件写过的前缀（带 WJ 标记的 `1.1`） | 旧前缀被盖住，只显示一层虚拟编号；该编号用 stale 样式，悬停有「文件里还留着旧编号」提示 | ✅ 区间计算；渲染待周期 2 |
| V14 | 在 V13 的文件上执行「清除本文件残留编号」 | 只剥 WJ 标记的前缀，手写编号保留；不写 `fm:false`；清除后虚拟编号恢复正常样式 | ✅ 纯函数 + 命令（`main.test`）；只在仅显示文件里出现 |
| V15 | 路径规则从「写入」切到「仅显示」并确认 | 确认框里的 N 等于有效规则就是这一条的文件数（被更具体规则覆盖的不算）；确认后这些文件的 WJ 前缀被清除，手写编号保留，不写 `fm:false`；已打开的文件可撤销 | ✅ 规划与清除（`main.test` / `modeswitch.test`）；确认框交互待手验 |
| V16 | V15 中被清除的标题在其他笔记里有链接 | 链接随清除一并更新（批量通道的 backlink 同步），不断链 | ✅ |
| V17 | V15 的确认框点取消 | 规则 mode 不变，没有任何文件被改 | ✅ 逻辑（规则在副本上试改）；真机待手验 |
| V18 | 路径规则从「仅显示」切到「写入」，勾选「立即对这 N 个文件写入编号」/ 不勾选 | 勾选：批量写入编号（含 backlink 同步）；不勾：文件不动，下次编辑时再写 | ✅ |
| V19 | 删除一条写入规则、改写入规则的路径、把写入规则改成「不编号」，且会让 N>0 个文件的有效模式从写入变过去 | 弹同一确认框，提供「清除」与「保留编号（冻结现状）」；改成「不编号」默认冻结 | ✅ 差异计算；确认框交互待手验 |
| V20 | 把已写过编号的文件从写入文件夹挪进仅显示文件夹 | 不弹框；按 V13 显示残留提示 | 🔲 |
| V21 | 仅显示文件里手动改一个被其他笔记引用的标题 | 走 backlink 独立同步分支，其他笔记里的链接跟随；不写入任何编号 | ✅ |
| V22 | 对仅显示文件执行「立即重新编号」 | 只刷新显示，弹 Notice「此文件为仅显示模式」，文件不变 | ✅ 不写 + Notice；刷新显示待周期 2 |
| V23 | 仅显示文件上使用标题链接建议、VC 联动 | 照常可用；锚点是不带编号的纯文本标题 | 🔲 |
| V24 | 「固化编号并交还所有权」遇到仅显示文件 | 照常剥 WJ（含链接与残留，跳过会断链）；按钮说明写清楚「仅显示的编号会随之消失」（周期 1 订正） | ✅ 行为沿用既有测试；说明文案已加 |
| V25 | 修改模板、设置或路径规则 | 所有已打开编辑器与阅读视图里的虚拟编号立即刷新 | ✅ 广播逻辑（`main.test`）；真机观感待手验 |
| V26 | CM6 位置映射：在标题上方插入 / 删除若干行，去抖重算之前 | 装饰位置经 `map` 跟随，不漂移、不错位（node 环境测 DecorationSet） | ✅ `virtual-render.test` |
| V27 | 实时预览、源码模式、阅读视图三种视图 | 实时预览与阅读视图显示编号，**源码模式只显示原文**（真机实测后订正）；在标题开头打字编号不跳 | ✅ 真机 |
| V28 | 中文拼音输入法在仅显示文件的标题里组合输入 | 组合期间不重算、不打断输入，上屏后编号补算正确（手验） | ✅ 用户真机 |
| V29 | 阅读视图复制标题文字；嵌入（`![[note#section]]`）与悬浮预览 | 复制内容不带编号；嵌入与悬浮预览里编号正确，对不上时不显示，不出现错号（手验） | ✅ 真机：复制不带编号、整篇与小节嵌入、整篇与小节悬浮预览编号均正确 |
| V30 | 内置「导出 PDF」 | 导出结果带虚拟编号（手验） | ✅ 真机 |
| V31 | 移动端（iOS / Android 任一台） | 三种视图都显示编号，编辑正常（手验） | 🔲 |
| V32 | 约 2000 个标题的长文件里连续打字、滚动阅读视图 | 不卡顿；阅读视图缓存命中，不按块重复整篇计算（手验 + 计数单测） | ✅ 缓存计数单测；卡顿与否待手验 |
| V33 | 在仅显示文件里粘贴从已编号标题复制来的内容 | 不还原带 WJ 的原文，只接受净化后的文本（否则等于制造残留） | ✅ |
| V34 | 在仅显示文件上执行「清除当前文件编号」 | 清掉残留，但不写 `fm:false`（否则会关掉虚拟显示） | ✅ |
| V35 | 批量重编号的规则范围里有被更具体的仅显示规则覆盖的文件 | 这些文件被跳过、计入「跳过」数 | ✅ |
| V36 | UVM：随机序列里翻转规则模式（写入 ↔ 仅显示） | 仅显示文件触发时显示的编号与写入模式逐行一致、文件不被改写；覆盖率含「残留状态下触发」 | ✅ |
| V37 | 回归（真机实测）：阅读视图渲染过后，在编辑模式里于前面插入 / 删除标题，再切回阅读视图 | 全篇编号一致，没重渲染的段落也改成新编号，不出现新旧编号混杂 | ✅ 单测 + 真机 |
| V38 | 回归（真机实测）：只有「仅显示 → 写入」的模式切换 | 不执行清除，不弹「已清除 0 个文件」 | ✅ |
| V39 | 回归（真机实测）：小节嵌入 `![[笔记#标题]]` | 按全文编号（如「三、概述」），不从「一」重数 | ✅ 单测 + 真机 |
| V40 | 仅显示文件打开时看 Obsidian 内置大纲面板（1.2.0，原 M14 二期） | 每个标题前显示与编辑器一致的编号（同一模板、前后缀与样式），不编号的标题不加；写入模式文件（编号本在文字里）与「不编号」文件的大纲不加 | ✅ 单测（`virtual-outline.test.ts`）+ 真机（1.2.0：基础 / 残留 / 大文件三篇逐条与编辑器一致，H1、白名单、跳级标题同样不编号） |
| V41 | 大纲开着时改文件：增删标题、调层级、在大纲里拖动小节；在大纲里搜索过滤、折叠 / 展开；长大纲滚动 | 大纲刷新后编号随之更新；过滤、折叠、滚动后编号仍在且正确——大纲只渲染可见条目，编号挂在条目对象上，不靠扫描屏幕上的 DOM 对齐 | ✅ 单测 + 真机（搜索过滤、折叠 / 展开、最前面插入标题后顺延、2000 个标题的大纲跟随光标滚到末尾）；在大纲里拖动小节走同一刷新路径，未单独测 |
| V42 | 带残留旧编号（以 WJ 开头）的标题 | 大纲里不叠加虚拟编号：大纲只能显示文件原文，叠上去就是两层数字；编辑器里照常用虚线编号提示残留 | ✅ 单测 |
| V43 | 设置「在大纲中显示编号」开 / 关；停用插件 | 关：大纲编号立即消失；开：立即出现；停用插件后大纲恢复原样、不留属性 | ✅ 单测 + 真机（设置开关即时生效）；停用插件由单测覆盖 |
| V44 | 大纲显示的不是当前活动文件（固定的大纲、关联到另一个窗格） | 按大纲自己所示的文件计算编号 | ✅ 单测 |
| V45 | 取不到大纲的内部结构（Obsidian 更新改了内部实现） | 什么都不做、不报错；编辑视图与阅读视图的虚拟编号不受影响 | ✅ 单测 |
