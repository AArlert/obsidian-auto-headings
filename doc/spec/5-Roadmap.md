# 5. Roadmap

> **执行顺序（2026-07-10 重排，动机与逐项论证见 [附录 A](./A-方向审查与否决理由.md) §6；2026-09-25 再排：
> M14 虚拟编号模式插到第 1 位，依据是竞品分析——「只显示、不写文件」是当前最大短板）**：Milestone 编号是
> **历史标识符，不再暗示先后**；执行顺序以下表为准——信任与独立价值优先于体验纵深，"敢在重要库里装 +
> 被别的工作流依赖"先于"面板更好看"。
>
> | 顺序 | Milestone | 主题 | 状态 |
> |------|-----------|------|------|
> | ✅ | M0–M7 | 核心引擎 → 1.0.0 上架 | 已完成（已通过官方审核，商店正式上架） |
> | 1 | [M14 虚拟编号模式](#milestone-14--虚拟编号模式仅显示不写文件) | 「只显示、不改文件」，新装默认 | 开发中 |
> | 2 | [M11 信任包](#milestone-11--信任包插队-m8a-之前) | "敢在重要库里装"的全部前提（H8 清库竞态属数据安全，可随时插队小修） | 规划中 |
> | 3 | [M12 独立价值包 / infra 转向](#milestone-12--独立价值包--infra-转向) | 定位倒转：链接不断 = 零配置第一价值 | 规划中 |
> | 4 | [M8a 侧栏大纲导航](#milestone-8a--侧栏大纲导航只读低风险) | 留存型体验（获客不靠它） | 规划中 |
> | 5 | [M8b 拖放重排](#milestone-8b--拖放重排与行内编辑结构性变更高风险) | 高风险结构编辑 | 规划中 |
> | 6 | [M10 TOC burn-in](#milestone-10--原生风格-toc-burn-in) | 依赖 M8b 基础设施 | 构思中 |
> | 7 | [M13 标题链接建议 + VC 联动](#milestone-13--标题交叉引用自动补全--various-complements-联动) | 输入即链接（1.0.26 已落地，真机手验待回填） | 规划中 |
> | — | [M9 候选池](#milestone-9--后续候选功能) | 不排序的候选蓄水池（2026-07-10 已清池一轮） | 常设 |

## Milestone 0–7 — 核心引擎到 1.0.0 上架（已完成）

> **2026-07-10 压缩**：八个已完成里程碑的逐项 checklist 压缩为单行摘要——设计细节详见本文 §2/§3
> 各功能节（单一事实源），周期过程见 `log-archive.md`。

| Milestone | 主题 | 关键落地 |
|-----------|------|----------|
| M0 | 项目初始化 | 示例插件脚手架、TS/ESLint/Prettier、最小 `main.ts`、设置页骨架 |
| M1 | 解析与计数引擎 | `parser.ts` 标题解析（代码块边界）、计数器状态机、单元测试 |
| M2 | 写入与防抖 | onChange 防抖、单事务整文件重写、`topLevel` 取代 H1 降级（永不改 `#` 层级）、全局开关/命令、frontmatter 单文件开关 |
| M3 | 模板系统 | 模板 schema 校验与 `templates/*.json` 持久化、五种序号样式、继承前级、GUI 模板增删 + 行内编辑面板 |
| M4 | 白名单（模板级） | 三种匹配（全部/部分/子树）+ 归一化 + 并集解析、默认词表 16 条、白名单编辑器 + 实时命中预览 |
| M5 | 按路径配置 | 路径规则表（具体度解析、`/` 根规则即全局默认）、开关两层化、frontmatter `true` 强制语义、删模板知情确认与安全降级 |
| M6 | 清除与健壮性 | 清除当前文件/全库、全样式并集剥离器、`bottomLevel` 编号区间、中英双语（0.6.5）、防抖可配、边界情况加固 |
| M7 | 上架冲刺 | Backlink 同步（编号触发 + 纯文本改名快照基线，见 [3.12](./3.12-Backlink同步.md)）、多 TAB 设置页（[3.13](./3.13-设置面板多TAB.md)）、双语 README、独立发布仓库 + tag 触发 Release 自动化、manifest id 合规改 `auto-headings`（frontmatter 键与仓库名不变）、1.0.0 经 Community Hub（community.obsidian.md，2026-05 起取代 obsidian-releases PR 机制）提交并**通过官方审核正式上架** |

## Milestone 14 — 虚拟编号模式（仅显示，不写文件）

> **执行顺序第 1 位**（2026-09-25 立项）。规格见 [3.22](./3.22-虚拟编号模式.md)，验证见 testplan V 组。
> 一个功能分支分 5 个周期提交，每周期写 log 块、跑 preflight；周期 1、2 动核心逻辑，额外跑 `test:fuzz`。
> 周期 1 即 `npm run bump minor` 到 1.2.0（避免真机测试与线上 1.1.4 混淆），tag 等周期 4 再打。

- [x] **周期 0｜文档先行**：§2.2 翻案、§3.22 定案、Roadmap 重排、M9 两条旧候选并入本节、testplan V 组（2026-09-25）
- [x] **周期 1｜模型 + 纯逻辑 + 门控（无 UI）**（2026-09-25，1.2.0）：`PathRule.mode` 与归一化、新装判据（不立即落盘）+
      `onExternalSettingsChange`、`resolveNumberingMode`、`src/virtual/compute.ts`、`shouldAutoTrigger`
      对虚拟文件返回 false、新纯函数 `clearPluginNumberingContent`。测试：`settings`（判据 / 延迟落盘 /
      load 先于 templateStore.init）、`pathrules`（mode 解析与具体度）、新建 `virtual.test.ts`、`cleanup`
      （只剥 WJ）、`main`（虚拟文件不写入、只走 backlink 独立分支）、UVM 新 oracle「虚拟文件任意操作序列下
      字节不变」。显式把 `@codemirror/state` / `view` 加进 devDependencies（锁到 lock 里的版本）
- [x] **周期 2｜渲染**（2026-09-25，代码与单测完成；视觉 / 光标 / 输入法 / PDF 待真机）：`editorExtension.ts`（含输入法暂停）、`readingView.ts`（字符串比较缓存 +
      `getSectionInfo` 为 null 的兜底）、`.ah-virtual-number` / `--stale` 样式、「清除本文件残留编号」命令、
      设置 / 模板 / 规则变化广播刷新；obsidian-mock 补 `registerEditorExtension` /
      `registerMarkdownPostProcessor`；node 环境直接测「状态 → DecorationSet」与 map 后位置，不引入 jsdom
- [x] **周期 3｜UI 与切换**（2026-09-25，逻辑单测完成；确认框交互待真机）：路径规则行加 mode 下拉框（虚拟行隐藏或禁用批量重编号）、切换确认 Modal
      （含删规则 / 改路径 / 改不编号入口与冻结选项）、i18n 中英 key、立即重新编号的虚拟提示、固化按钮说明、
      `modeSwitch.ts` 测命中筛选与取消语义
- [ ] **周期 4｜对外文档与发版**（2026-09-25：文档部分已完成——README 中英、使用指南「两种模式」一节、release notes 1.2.0、manifest description；合并 master 与打 tag 待用户确认）：release notes 1.2.0（双语）；README 卖点加「可以只显示编号、完全不改
      文件」，FAQ「会往笔记里加隐藏的东西吗？」按模式分答；新增 FAQ「占资源吗？」（口径见 M12
      「README 资源与隐私承诺」）；user-guide 新增「两种模式」一节；manifest description 顺带重排（M12 项）；
      合并 master、打 tag
- [x] **内置大纲面板显示虚拟编号**（原二期候选，1.2.0 应用户要求提前）：条目对象上挂属性 + CSS 画编号、
      MutationObserver 跟刷新，设置可关，见 [3.22](./3.22-虚拟编号模式.md)「渲染」与 testplan V40–V45

## Milestone 11 — 信任包（插队 M8a 之前）

> **执行顺序第 2 位**（2026-07-10 立项；2026-09-25 让位 M14。动机与逐项论证见 [附录 A](./A-方向审查与否决理由.md) §2/§4/§5/§8）。
> 目标：把"敢在重要库里装"的全部前提补齐——对 WJ 风险从"披露"升级到"主动消解"、修完离场通道上的
> 已知隐患、把"静默改写他人文件"置于用户批准之下。多为小任务，合计工时预计小于 M8a。

- [ ] **Backlink 审阅模式**（决策 D2，自 M9 升格）：生成变更清单供用户批准后写入，替代静默改写；
      同时回应 [2.6](./2.6-已知生态兼容性风险.md)「Git diff 噪音」与批量误改焦虑
- [x] **H8 修复**（1.2.0，testplan H8/H17–H19，自 testplan backlog 与 M9 升格）：`clearAllVaultNumbering`
      / `freezeVaultNumbering` 改走批量通道 `batchRewrite`——已打开的文件走它的编辑器事务（不读盘、
      不覆盖未落盘改动，可撤销），未打开的走 `vault.process` 原子读改写；顺带同步指向这些标题的内链
      （同文件自链接随主事务折叠，跨文件反链汇总一条 Notice，反链引用方已打开时同样走它的编辑器）
- [ ] **清库撤销**（自 testplan backlog，与 H8 同段代码升格、这次未一并做）：危险区面板追加「撤销上次
      清除全库编号」——操作前把受影响文件内容快照进内存，插件自建快照/还原（非 `Ctrl/Cmd+Z`；已打开
      文件那部分现在确实进了该文件的编辑器撤销历史，但未打开文件走 `vault.process` 仍不在任何撤销
      历史内，这条独立的「自建快照/还原」仍有价值）
- [x] **复制净化开关**（1.0.10 已实现，设计见 [2.8](./2.8-剪贴板净化设计.md)）：
      同步净化 + 内存映射双通道——copy/cut 出口 `text/plain`（及已有的 `text/html`）净化剥 WJ，
      `净化文本 → 原文` 映射记入插件内存 LRU；`editor-paste` 同步查表命中即还原原文、避免双重
      编号，未命中完全放行 Obsidian 默认粘贴管线；任何一步失败静默维持现状；文件不变、渲染不变。
      **1.0.16 起取消开关、恒开**（理由见 [2.8](./2.8-剪贴板净化设计.md)「无开关」）。
      [2.6](./2.6-已知生态兼容性风险.md)「跨平台剪贴板投毒」已改记「主动消解已实现」；
      真实客户端表现矩阵与移动端实机（testplan O4/O8/O10）待回填
- [x] **导出验证矩阵**（吸收原 M9「带编号的导出」调研项）：Pandoc 侧**已实测完毕**
      （2026-07-19，pandoc 3.10 + typst 0.15.1，testplan O5a–O5e 全 ✅）——复现了
      `--number-sections` 双重编号，交付了**可用的** filter
      [`assets/pandoc/strip-autoheadings.lua`](../../assets/pandoc/strip-autoheadings.lua)
      （双模式、保内联格式、兼容旧单哨兵），并证实该路径 PDF 文本层无 WJ 残留；
      附带定论：`##` 起头文档 pandoc 按嵌套深度编号，与默认 `topLevel=H2` 恰好吻合。
      结论已回填 [2.6](./2.6-已知生态兼容性风险.md) 与两版 README「导出与外发」节。
      **注**：`marker-contract.md` §3 原 filter 初稿有实缺陷（`pandoc.utils.stringify`
      压平标题内联格式），本次弃用并替换为指向仓库内 filter 的实测配方。
      剩余两格不在开发机能力内，转手验：内置「导出为 PDF」（O5f，待用户真机）、
      Publish 锚点（O5g，无订阅）
- [ ] **大库性能实测**（testplan M12 场景）：高引用标题改名时串行 `vault.process` 的体感；
      实测后在契约/README 立性能预算数字
- [ ] **CM6 原子区域**（自 [2.5](./2.5-双哨兵自愈.md) 方案表"留作后续"升格，
      系统论证见 [附录 A](./A-方向审查与否决理由.md) §8.4）：前缀编辑器内只读、光标跳过——WJ 之上的第四层防护 +
      "编号是托管的"心智模型；设独立开关（默认关一个版本，实测稳定后翻默认）；
      「前缀可手改」原则（[2.3](./2.3-边界情况与设计决策.md)）同步修订；交互细节（整段删除放行 /
      自家写回豁免拦截 / Vim / 编辑器内搜索替换 / IME 边界 / 移动端实机）逐一拍板并登记 testplan O6
- [ ] **Canvas 引用方拍板**（[2.6](./2.6-已知生态兼容性风险.md) 新行）：`syncBacklinks` 显式跳过
      `.canvas` 或显式支持 + 测试——现状行为靠巧合，testplan O1
- [ ] **外部写入陈旧快照评估**（[2.6](./2.6-已知生态兼容性风险.md) 新行）：`vault.on("modify")`
      刷新 `headingSnapshots` 的成本与竞态面评估，testplan O2
- [ ] **E8 拍板**（testplan E8）：行首 ≤3 空格缩进标题——跟随 CommonMark/Obsidian 解析，或维持
      现状并写进规格，二选一定案

## Milestone 12 — 独立价值包 / infra 转向

> **执行顺序第 3 位**（2026-07-10 立项；2026-09-25 让位 M14。动机见 [附录 A](./A-方向审查与否决理由.md) §1/§7）。目标：完成定位
> 倒转——"链接永不断"成为零配置的第一价值、编号降级为可选进阶功能；并把改名引擎从内部实现开放为
> 可被依赖的服务。

- [x] **CR-18：Backlink 同步独立于编号模板触发**（自 M9 升格，规格见
      [3.12](./3.12-Backlink同步.md)"独立于编号模板的触发"）：不依赖 `getTemplateForFile`
      的触发路径，复用既有纯函数与快照基线，不新增编号逻辑——**1.0.8 落地**独立开关
      `backlinkStandaloneTrigger`（默认关）；**1.0.9 并入 `updateBacklinks` 单开关全局生效**（两层
      开关对用户是无谓认知负担，合一后开启即始终触发）；常规编号路径已处理本轮时该路径不重复跑；
      testplan M19–M26
- [x] **多文件批量重编号**（自 M9 升格，UX 已定案）：路径规则表格行内入口（对该规则匹配到的全部
      文件生效）+ 确认对话框——**1.0.13 落地**（规格见 [3.8](./3.8-按路径配置.md)，testplan K16：逐文件
      按各自解析的模板、尊重 fm `false`/外来编号守卫/「不编号」、编辑器/vault 双写回通道）
- [x] **路径规则「不编号」伪模板**（自 M9 升格）：文件夹级彻底关闭编号，替代逐文件 frontmatter
      `false` 的高成本维护——**1.0.13 落地**（规格见 [3.8](./3.8-按路径配置.md)，testplan K15：哨兵
      `$none` 参与具体度解析并可胜出，已有编号冻结不动）
- [x] **注释块跳过**（自 M9 升格，1.0.14）：`%%…%%` 与 `<!-- -->` 内的 `#` 行不视为标题——
      不编号、不推进计数器、不进 backlink 快照。规格与十条边界裁决见
      [3.17](./3.17-跳过区域.md)，含**区域内残留的分区域分治**（注释块自动清、
      围栏只在清除命令时清）。实现收敛到新模块 `src/scan.ts`（原先 `parser.ts` 与
      `numbering.ts` 各有一份同构围栏状态机）。testplan E19–E29
- [x] **固化编号并交还所有权（全库）**（1.0.14）：编号保留为普通文本、移除全部标记、插件停止
      接管，给「喜欢现在的编号但不想再被插件管着」与「卸载但要留住编号成果」一条干净的离场路。
      规格见 [3.18](./3.18-固化编号并交还所有权.md)，testplan H9–H12
- [ ] **全库「扫描修复历史断链」命令**（自 M9 升格）：与 WJ 方案配套的安全网，补"开关只管从此刻起"
      的历史缺口
- [ ] **内置三套预设模板：学术 / 书稿 / 公文**（2026-09-25 登记，对标 Number Suite 的层级 / 公文 /
      法律三套方案）：新用户开箱即可选，不必自己拼模板
- [x] **README 资源与隐私承诺**（2026-09-25 登记并随 M14 周期 4 落地，借鉴 Heading Keeper / Number Suite 的写法，随 M14
      周期 4 一起落）：只写已核实的事实——不联网、不收集数据（`src/` 无 `requestUrl` / `fetch`，发版前
      再查）；**启动时在本地读取一次全库标题用于链接建议（可在设置里关掉），之后编号只处理正在编辑的
      笔记**（注意：「从不扫描全库」**不成立**，见 `buildInitialHeadingIndex`）；内存有上限（标题索引 5 万条、
      剪贴板缓存约 2MB）
- [x] **manifest description 卖点重排**（自 M9 升格；2026-09-25 随 M14 改为「可只显示 / 写入 + 链接跟随」打头）：backlink 同步前置为第一句；manifest 属产物，
      按上架后策略随下一个行为版本一并 bump
- [ ] **公开 API 第一步：标题改名事件**：改名表产出后广播（workspace 自定义事件或插件实例公开方法
      + 类型声明），让 Tasks / Dataview 脚本 / 其他大纲、编号插件消费"某文件某行标题 X→Y"——
      改名检测引擎作为服务的价值大于作为内部实现
- [ ] **《从 Number Headings 迁移》指南 + 社区发布**：README 迁移节已建（2026-07-10），补功能对照
      长文与论坛/Reddit 发布——竞品 8.5 万下载、停更约 2.5 年，「清理非本插件编号」命令即现成迁移
      工具，零代码增长
- [ ] **迁移向导自动配置**（源自 [A.11](./A.11-竞品格局核验.md)
      竞品核验，2026-07-18）：可用「样式检测 + 一致性投票」自动识别既有编号样式并预选模板
- [x] **两条借鉴命令：复制编号大纲 / 复制当前小节链接**（源自 [A.11](./A.11-竞品格局核验.md)
      竞品核验，2026-07-18）——**1.2.0 落地**：与 backlink 反链能力形成复制/分享闭环；写入模式取
      标题所见文本，仅显示模式取虚拟编号，两种模式都可用；命令入口见 [3.1](./3.1-开关命令与生效判定.md)，
      testplan R1–R5（R6 链接解析与改名同步真机手验待做）
- [x] **单标题跳过编号，phase 1：识别**（用户 [issue #6](https://github.com/AArlert/obsidian-auto-headings/issues/6)，
      2026-07-29 提出、2026-08-08 已回复用户承诺后续版本）——**1.0.17 落地**（规格见
      [3.21](./3.21-单标题跳过编号.md)，testplan E30–E36）：白名单按**标题文本**
      匹配，管不了「同一个词在这篇要跳过、在别篇要正常编号」；补的行级豁免是标题行尾手写
      `<!-- skip -->`（大小写/空白容错），命中即跳过且不推进计数器，接到白名单豁免同一分支。
      **phase 1 只跳本行、不含子树**（是否加子树选项待真实反馈）；内链锚点表现待实机（E36）。
- [ ] **单标题跳过编号，phase 2：GUI 入口**：等标题旁浮动菜单（M8a/M8b 方向）落地后，做成一键
      命令——点击切换该标题是否 skip，标记由插件写入（burn-in），用户不必记语法。
      > 定位纪律：**手写标记只是兜底能力，不是主要用法**，README 不得把「写一行标记」当卖点售卖；
      > 同 [3.20](./3.20-单文件配置的落盘位置.md) 的判据——用起来轻松才是正道。
- [ ] **Setext 式标题（`===` / `---` 下划线）支持**（2026-08-09 登记，**优先级待定**）：现状
      [`parser.ts`](../../src/parser.ts) 的 `HEADING_RE` 只认 ATX（`#` 前缀），Setext 标题**整段漏编**，
      从 Word / 其他 Markdown 工具迁入的文档受影响。做法是 `parseHeadings` 前视一行判定 H1/H2，
      `scan` / `backlinks` / `cleanup` 全链路自动受益，与 WJ 双哨兵无冲突。
      **必须连坑一起做**：frontmatter 结尾的 `---` 极易被误判成 Setext H2，导致正文首个 H1 从 2
      起编（竞品 gurjar1 的同类缺陷至今 open），实现时须有对应用例。

## Milestone 8a — 侧栏大纲导航（只读，低风险）

> 目标：先把「原生风格侧栏大纲面板」的导航价值（展示 + 定位 + 搜索 + 跳转）独立交付，不依赖高风险
> 的结构编辑（拆到 M8b）。详细规格见 [3.14](./3.14-侧栏大纲导航.md)。

- [ ] 注册自定义 Leaf Type + 面板外壳（⚙️ 设置入口 + 🔍 搜索框）
- [ ] 动态层级滑块（H1–H6 按当前文件动态禁用 + 展开/折叠过渡，先落地固定时长）
- [ ] 标题树 + 当前位置自动定位 + 保留树形结构的搜索过滤（上下键 / 回车 / Esc）
- [ ] 当前位置 / 搜索匹配高亮（深浅色主题皆可读）
- [ ] 双击跳转、右键菜单（Copy link / Copy as Markdown list / Expand all / Collapse all）、键盘导航、面板横向滚动
- [ ] 大文件虚拟化 + 搜索防抖 100ms + `editor:changed` 实时刷新
- [ ] 测试基建：新增面板测试文件按需引入 `jsdom`（`// @vitest-environment jsdom`），覆盖树构建 / 搜索过滤 / 键盘导航；动画与真实渲染效果仍靠 `user_tests` 手验

## Milestone 8b — 拖放重排与行内编辑（结构性变更，高风险）

> 目标：在 M8a 之上补齐"结构重组"能力，成为唯一集「编号维护 + 结构重组」于一体的 Outline 工具。
> 详细规格见 [3.15](./3.15-拖放重排与结构编辑.md)。风险高于以往任何一个 Milestone——新增「剪切—拼接」
> 结构性写入路径、边界情况数量级预计超过 M6/M7 已加固的"原地改标签"场景；建议独立排期，不与 M8a 同批验收。

- [ ] 拖放重排：剪切—拼接—重新编号—同步 backlink 的一次性 transaction + 撤销支持；拖放期间临时禁用编辑器避免并发冲突
- [ ] 边界约定：禁止拖入自身/子孙、代码块；白名单子树内**直接禁止拖入**（已在 [3.15](./3.15-拖放重排与结构编辑.md) 定案，不做"允许但弹警告"的中间态）
- [ ] 拖放视觉反馈（起拖高亮、目标提示、完成闪烁）
- [ ] 交互面：主编辑器 gutter 追加升降级按钮（∧/V）+ 拖放把手，与 Obsidian 原生标题折叠三角共存、不改变原有布局（0.7.28 补充设计，见 [3.15](./3.15-拖放重排与结构编辑.md)）；升降级按钮是否级联调整子标题层级为未决问题，需详细设计阶段拍板
- [ ] 行内编辑模式（隐藏编号、支持加粗/斜体/链接/代码快捷键，提交后重编号 + 同步 backlink）
- [ ] 移动端触摸长按拖拽（预期需要独立的移动端实机验证周期，参考 M7 MOB-1）
- [ ] 架构落地前置：把"结构变更执行"实现为独立纯函数（如 `moveHeadingBlock`，不碰 DOM），使其可并入 `tests/dev_tests/uvm/` 的约束随机序列做回归，而非只能手工验证

---

## Milestone 9 — 后续候选功能

> **2026-07-10 清池**（[附录 A](./A-方向审查与否决理由.md) §6）：九项候选升格离池——审阅模式、H8+清库撤销、
> 「带编号的导出」（→ 导出验证矩阵）进 [M11 信任包](#milestone-11--信任包插队-m8a-之前)；断链修复
> 命令、批量重编号、「不编号」伪模板、注释块跳过、description 重排、Backlink 独立触发进
> [M12 独立价值包](#milestone-12--独立价值包--infra-转向)。「阅读模式下插件运行」被新候选
> 「虚拟编号模式」吸收。各升格条目的历史表述（UX 定案、动机注记）已随迁至新 Milestone，此处不留副本。

既有完成项（已落地）：
- [x] **起始编号数字 `startIndex`**（模板级，默认 1；设 0 得 `0.1.1`）——0.7.12
- [x] **白名单编辑器 Filter / Sort**（搜索框 + 排序）——0.7.12

后续候选功能：
- [x] ~~**虚拟编号模式**~~ → 2026-09-25 立项为 [M14](#milestone-14--虚拟编号模式仅显示不写文件)，规格见 [3.22](./3.22-虚拟编号模式.md)
- [ ] **把虚拟编号一次性烧录成纯文本**（2026-09-25 登记，自 M14 拆出）：对仅显示模式的文件一次性写入当前
      显示的编号并交还所有权（不带 WJ），供「最终稿要带编号外发」的场景
- [ ] `skipFill` 补渲染预览——fill/drop/none 三种模式各给一个"H3→H5 跳级"的具体示例渲染（如 fill 显示
      `1.1.0.1`、drop 显示 `1.1.1`），替代纯文字描述，落实 [3.13](./3.13-设置面板多TAB.md)"预览优先"原则
- [ ] **Dataview 集成**——不自建"生成目录"命令（与 [2.2](./2.2-非目标.md) 非目标一致，M8a/M8b 已经是本项目
      在"导航/结构"方向的主投入，不重复铺一条"生成"产品线），而是保证本插件产出对 Dataview 友好，
      让用户自己用 DataviewJS 拼跨文件目录/索引：① 确认编号前后的哨兵字符（U+2060 Word Joiner）在
      Dataview 展示/字符串匹配场景下的行为，必要时文档给一段"如何在 dataviewjs 里 strip 掉 WJ"的
      示例（剥 WJ 与剥整前缀的通用片段已入 [`marker-contract.md`](../marker-contract.md) §3，可直接
      引用）；② frontmatter 开关键 `obsidian-auto-headings` 本身就是可查询字段，补一段示例查询（如
      列出所有被单文件覆盖为 `false` 的笔记）；③ 待验证 Dataview 是否有开箱即用的标题字段，若没有
      则文档给出推荐的 DataviewJS 片段（如通过 `app.metadataCache.getFileCache(file).headings`）。
      本项定位是"验证 + 写文档"，不是新增插件代码
- [ ] 「预览模式」——写入前预览编号效果
- [ ] 一键复制模板白名单到另一个模板

### 竞品源码级调研的采纳清单（2026-08-09 立项，原始报告见 `doc/research/`，**不入库**）

> 调研方式：本机 codex CLI 离线读五家竞品 clone 的源码 + subagent 挖 6 个仓库 124 条 issue。
> **原始报告按仓库口径不入库**（含逐条点名的缺陷与源码行号，公开发布仓库放不得，见
> `doc/research/README.md`）；此处只留**决策**——采纳什么、为什么、代价多大。排序 = 值得做的程度。
> 每条都还未立项开工，落地前需按本文档既有惯例先补规格节 + testplan 行。

- [x] ~~**只读「装饰预览」通道**~~ → 与「虚拟编号模式」是同一件事，已并入 [M14](#milestone-14--虚拟编号模式仅显示不写文件)
- [ ] **阅读视图 live TOC**（gurjar1 的 ` ```toc ` 代码块处理器；中 / 低）：随标题增删自动更新、
      不污染正文、移动端可用。用户 issue 里「skip + 自动目录」被当成旗舰组合（`onlyafly#55`）。
      **注意与 [2.2](./2.2-非目标.md)「不自建生成目录命令」的边界**：那条否决的是「往正文里生成一份
      物理目录」，而这条是**只读渲染、不写文件**，性质不同——真要做须先在 §2.2 补一句澄清。
      锚点必须用去 WJ 的干净标题（可复用现有 `linkAnchor`）。
- [ ] **光标保护升级为「脏标题行集合 + 离开该行才重排」**（hobeedzc；中 / 中）：原始诉求是保护面
      窄（粘贴多行、改非光标行标题时仍会被动）。改动核心触发链，回归面大。**1.0.23 走的是相反
      方向**：真机反馈暴露的全是「保护面**过宽**」导致的卡顿感（改层级 / 新敲标题都等不到
      编号），故一路**收窄**到只护行尾空白、不再冻结任何行（详见 [3.19](./3.19-单文件暂停与不抢键盘.md)
      「换掉整行冻结」）。**本条的覆盖面扩展仍未做**，且现在要重新评估必要性——「离开该行才重排」
      依赖的光标离开事件正是 1.0.23 绕开的那条路（自动路径只挂 `editor-change`，移动光标不触发
      任何事件），真要做得先引入 CM6 `updateListener` 一类的选区监听，成本与回归面都要重新估。
- [ ] **backlink 批量改写失败回滚**（hobeedzc；中 / 中）：跨文件批量改写最怕半途失败造成「部分
      改了、部分没改」。**但不可照抄其实现**——它是「并行写 + 整文件回滚」，并发写与用户编辑会
      竞态、回滚可能覆盖用户新内容；正确做法是顺序写 + 记录已改清单，失败时只回滚本次清单。
- [ ] **「清理非本插件的标题编号」升级为分级预览**（借鉴 gurjar1 的置信度分级；中 / 低）：高置信度
      直接清、低置信度（如 `2024 `）列出让用户确认。**只进手动命令**，自动路径维持 WJ 保守判据，
      不重蹈其「默认接管低置信度编号把 `2024 Report` 吃掉」的覆辙。1.0.17 的清理预览确认框
      （[testplan J14](../testplan.md)）已经是这条的第一步；1.0.23 的逐条勾选（[testplan
      J17](../testplan.md)）又进一步——但那是**全量手动**（默认全勾，用户自己挑），并非本条设想的
      **按置信度自动分级**（高置信度免勾、只有低置信度才需要用户确认）。后者仍未做：现状是「一
      个都不用管」与「每条都要看」之间没有中间态，条目多时逐条核对仍累。
- [ ] **中文大写数字序号样式**（yalvhe2009 有、本插件 7 种样式里没有；低 / 低）：壹贰叁/拾佰仟，
      合同与公文场景常用。照现有 `toCJK` 加一套映射 + `render.ts` 增一种 `NumeralStyle` 即可。
- [ ] **自动起始层级**（hobeedzc / yu-zou；低 / 低）：数一遍文件实际用到的最浅标题层级作为起点，
      「只写 H3–H5 的笔记」无需配置即可从 H3 起编。做成**模板级可选项、默认关**——与既有「多 H1
      重启」语义有交互，默认开会改变已上架行为。
- [ ] **快速配置 Modal + 状态栏统计**（gurjar1；低 / 低）：把高频调整从多 TAB 设置页里解放出来。
      纯 UI 增量，与引擎无关。

> **调研同时产出的两条订正与一条已落地**（不属候选，记此备查）：① 附录 A.11 两处「移动端独占 /
> 唯一」错误论点已订正（实测五家只有 gurjar1 是 `isDesktopOnly: true`，1.0.16）；② 用户 issue #6
> 「单标题跳过编号」已于 1.0.17 落地 phase 1（[3.21](./3.21-单标题跳过编号.md)）；
> ③ Setext 标题支持已登记进 [M12](#milestone-12--独立价值包--infra-转向)（优先级待定）。

明确**不做**：标题居中 / 靠右、自定义字体（与 burn-in 哲学冲突，见 [2.2](./2.2-非目标.md)）。
**不采纳**的竞品做法：自动接管低置信度手写编号（gurjar1 默认行为，实测会把 `2024 Report` 吃成
`Report`）、纯正则识别编号（hobeedzc / onlyafly / yalvhe2009 共有，`## 2024 总结` → `## 1 总结`
类数据丢失的根源，正是本插件 WJ 双哨兵方案存在的理由）。

---

## Milestone 10 — 原生风格 TOC burn-in

> 详细规格见 [3.16](./3.16-原生风格TOC-burn-in.md)。构思阶段（0.7.28），尚未拆解到可估工时的
> checklist 粒度——先完成未决问题的拍板（围栏语法 / 链接形式 / 多块支持 / 白名单是否收录），再拆解
> 成与 M8a/M8b 同等粒度的任务清单。排期不早于 M8b 的"允许增删行的整文件重写"基础设施到位（两者共享
> 同一类"打破现有整文件重写不变量"的风险，见 [3.16](./3.16-原生风格TOC-burn-in.md)"关键约束"一节）。

- [ ] 未决问题拍板：围栏语法参数 / 目录条目链接形式（复用 `backlinks.ts` 的 `displayAnchor`）/ 单文件
      是否允许多个 `toc` 块 / 白名单豁免标题是否收录
- [ ] 光标 / 视图位置保持：验证 CM6 事务的位置映射在"TOC 块行数变化"场景下是否天然生效，含垂直滚动
      锚点
- [ ] 层级折叠判定逻辑与 [3.14](./3.14-侧栏大纲导航.md) 的动态层级滑块共享同一个纯函数，不重新实现

---

## Milestone 13 — 标题交叉引用自动补全 + Various Complements 联动

> **2026-08-11 立项**（完整实施方案见 `doc/research/` 本地调研留档，已按本仓库纪律留本地不入库）。
> 目标：把「输入即链接」变成默认能力——打字命中 vault 内已有标题原文即可 Tab/点按生成正确链接，
> 同时联动生态位更大的第三方补全插件 Various Complements（其自身「标题级补全」issue #72 开出 4 年多
> 未解决），以此触达其用户群，前提是联动本身足够稳健、不破坏用户已有的 VC 配置。
> **架构注**：全 vault 前缀匹配天然要求一份「vault 级标题 → 位置」索引，必然需要一次全库扫描
> （懒加载到 `onLayoutReady`）——与「从不为全库扫描」的既有承诺直接冲突，已随本里程碑修订
> （见 [§4 架构设计](./4-架构设计.md)「触发的性能边界」的 M13 例外）。

- [x] 全 vault 标题索引（`headingindex.ts`：排序数组 + 二分查找、增量维护、内存/性能上限
      50,000 条 / 单文件 500 条、截断时一次性 Notice 不静默丢弃）
- [x] `EditorSuggest` 建议框（`headingsuggest.ts`：前缀增量匹配、桌面 Tab + 移动点按接受、
      上下文屏蔽礼貌规则、IME 组合不触发）
- [x] 链接锚点复用 `backlinks.ts` 的 `displayAnchor`，正确处理已编号标题的 WORD_JOINER；
      **1.0.27 产品决策**：接受建议时 alias 自动补全为剥编号前缀后的**完整标题名**
      （用户实测反馈「希望是完整的标题名而不是残缺的」，与 VC 词典 value 的 alias 形态一致）
- [x] Various Complements 联动（`vcintegration.ts`：手动引导 / 自动配置两模式、分层防御写入、
      schema 校验失败整体放弃），两模式均需用户显式确认后才开启
- [x] **VC 侧事实已对照源码核实（1.0.27，clone 留档 doc/research/vc-source-verification.md）**：
      词典 JSON 顶层必须是 `{"words":[...]}`（裸数组会让 VC 解析抛错、整个词典加载失败——这是
      用户实测「联动后打一个『交』无候选」的根因之一，1.0.27 已修复）；`customDictionaryPaths` /
      `enableCustomDictionaryComplement` 字段名与 reload 命令 id `reload-custom-dictionaries`
      全部确认；自动配置顺带把 VC 的 `customDictionaryMinNumberOfCharactersForTrigger` 置 1
      （VC 默认触发阈值 3，1–2 个字符不触发）
- [x] **词典轻量三件套（1.0.27）**：紧凑 JSON（无缩进，20,000 条 ≈ 2.2MB 封顶）+
      内容未变不写盘（内存缓存比对，标题无变化时 iCloud/同步零流量）+
      词典独立条数上限 `MAX_VC_DICTIONARY_ENTRIES = 20,000`（超出截断 + 一次性 Notice）
- [x] **启动词典同步（1.0.28）**：VC 只在启动与 reload 命令两个时机加载词典——已联动用户
      升级/重启后，插件在 `onLayoutReady` 主动重写词典 + 调 reload（2s × 5 次重试，耗尽静默）
- [x] **M13 标题索引纳入 UVM 压测（1.0.28）**：`uvm/heading-index.ts` 参考模型记分板
      （朴素 Map + filter + 稳定排序对拍 DUT 的排序数组 + 二分 + 增量维护），随 `npm test`
      跑 500×60、`test:fuzz` 跑 5000×80；已抓出并修复 U5–U7（同 key 顺序不稳定 /
      rename 覆盖残留 / 自身改名计数漂移，见 testplan §3.3）
- [x] **无标点连写的尾部词触发（Q22，1.0.29）**：中文正文没有词边界，触发词提取会把「一笔」
      和随后打的「事务」吞成一个 token，前缀匹配必然落空。匹配候选放宽为「整段 + 逐级后缀
      （长度 ≥ `MIN_QUERY_LENGTH`）」取第一个命中（`headingtrigger.ts` 的 `suffixCandidates` /
      `resolveTriggerQuery`，索引 API 不变）。**关键约束**：`onTrigger` 的替换区间起点必须同步
      收窄到被匹配上的那一段，否则接受建议会吃掉前面的「一笔」——UVM 记分板每步断言
      `start` 与 `text` 自洽
- [x] **与 VC 的建议框共存策略（Q23，1.0.29）**：Obsidian 的 `EditorSuggest` 同一时刻**只显示
      一个**弹框，本插件与 VC 抢同一个位置——1.0.28 及以前本插件恒优先，VC 自己的文件链接建议
      整个看不见（用户实测截图）。新增设置 `headingSuggestWhenVcActive`：**默认 `"yield"`**
      （VC 已启用时本插件不触发，标题建议改由 VC 框经词典联动统一呈现），`"own"` 恢复旧行为。
      让路 + 词典联动关闭 = 标题建议无处出现，设置面板显式警告而非静默失效
- [x] **VC 框里的标题候选补齐与观感统一（Q25/Q26，1.0.32）**：VC 的 `jsonToWords` 把
      `value`/`displayed` 对调，去重谓词对 customDictionary 只比 `value` ⇒ **同名标题必被砍到
      只剩一条**；`disambiguateVcDisplayed` 只给冲突项加 `(文件名)` 后缀（区分形态按组统一，
      后缀只能在尾部——VC 首字母桶键取 `value.charAt(0)`）。词条加 `description = 路径` 作第二行
      小字。自动配置额外清空 VC 的 `displayedTextSuffix`（其默认值就是字面量 `" => ..."`，是
      **全局显示项**，确认框单列一条说明影响范围）。VC 侧图标按词条 type 的 CSS `::before` 决定、
      无 per-词典钩子，覆盖会误伤用户自己的词典 ⇒ 不覆盖，改把**本插件建议框对齐 VC 观感**
      （图标改书架语义、第二行 `0.75em`/`--text-muted`）
- [ ] `doc/testplan.md` §Q 场景回填（Q4/Q5/Q9/Q10–Q15/Q21/Q23/Q25/Q26 等真机手验项待用户实机确认）

不是本里程碑目标（见调研方案 §10 Out of scope）：块级 `#^blockid` 匹配、模糊/拼音匹配、跨 vault
匹配、批量转换存量正文的命令、VC 联动关闭时的反向清理（不删除已生成词典、不撤销已写入 VC 的配置）。
